# Edit Log — 2026-09-16

## v138.1 — 2026-09-16 — Chore (deps): merge the production and development dependency bump groups

**Why:** Dependabot PRs #170 (production-minor-patch, 4 updates) and #171 (development-minor-patch, 7 updates) had both been open since 2026-09-14 and both failed the `Typecheck, lint, test, build` job. The failure was not caused by either bump — it was `max-lines-per-function` on `XrayReferrals` (1467 > 1450), which `ea2391f2` (#173) had already fixed on `main` after the two PRs were branched. Left alone they would sit red indefinitely on a stale base while the repository accumulated open branches. Merging them onto current `main` clears both PRs and both remote branches and gets the updates in.

**What changed:** Merged both dependabot branches into `main` locally. #170 merged cleanly. #171 conflicted with it on `package.json` and `package-lock.json` because both branched from the same base and both rewrote the `@types` block; the conflict was resolved by taking the higher version of each of the three conflicting entries rather than either side wholesale — `@types/react` and `@types/react-dom` were `^19.3.0` on the post-#170 `main` and `^19.2.18` / `^19.2.7` on the dependabot branch, so accepting that side verbatim would have been a downgrade. `package-lock.json` was regenerated from the resolved manifest instead of being hand-merged. `dist/index.html` rebuilt against the new dependency set.

**File:** `package.json`

**File:** `package-lock.json`

**File:** `dist/index.html`

**Before:**
```json
    "@types/node": "^26.4.1",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
```

**After:**
```json
    "@types/node": "^26.5.1",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
```

**Verification:** `npm run lint:ci` clean. `npm run typecheck` clean. `npm run test:run` — 483 files / 4493 tests passed. `npm run build` succeeded in 15.83s. `npm run check:bundle-size` — 4479.9 kB raw / 1440.3 kB gzip, inside the 30 MB / 10 MB ceiling. Resolved lock versions confirmed: `@types/node` 26.6.1, `@types/react` 19.3.0, `@types/react-dom` 19.3.0, `vite` 8.3.0, `vitest` 5.0.0.

**Lines:** 3 files, +1529 / -605

---
