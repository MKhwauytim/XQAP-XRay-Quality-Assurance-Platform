# Edit Log — 2026-08-22

## v112.1 — 2026-08-22 — Add (dev): seed the simulated workspace's action log so its filters are exercisable

**Why:** The action-log filter bar shipped in v112.0.0 could not be exercised in a browser at all: `?sim=1` seeds a population, sample, assignments, answers, template and users, but **no action-log entries**, so the viewer rendered `0 من 0 سجل`. Verified in Chromium — the controls render, there is simply nothing to filter. That also blocks the E2E suite, which will hit the same wall.

**What changed:** 171 `WorkspaceActionEntry` records seeded into `5-system/audit/actions/`, one file per actor, chosen so every filter dimension has something to narrow:

- **All 47 action types** appear at least once, so no checkbox in the grouped picker is a dead control. Both high-volume types are present in bulk (`answer-submitted` ×40, `label-override-changed` ×8), so the default view is 123 rows — **above the 100 page size**, meaning paging exists without touching the type picker, and enabling the hidden types takes it to 171.
- **7 actors with deliberately unequal counts** (56/30/19/19/18/17/12), so narrowing to one is visibly different from another.
- **21 consecutive days**, every day populated; an inner window catches 59 and leaves real rows on both sides. Types are scattered across days by a stride coprime with the window length, so the date and type dimensions are not correlated — a filter test cannot pass by accident.
- `target` and `details` populated on most entries, searchable in both KEYS and VALUES.

**The real writer is deliberately bypassed here — the only place the sim seed does so.** `appendWorkspaceAction` stamps `at: new Date().toISOString()` and a crypto `id` itself. `at` is fatal to the purpose: a log where every entry is "now" cannot exercise a date filter, which is the whole reason for seeding it; the random id separately makes cross-build determinism unassertable. Entries are therefore built directly, but the FILES still go through the real layer — same `getAuditActionsDir`, `actionsFileName`, `WorkspaceActionUserLogFile` shape and `safeWriteJson` call — and a test proves they round-trip through the real `readWorkspaceActions`. A later genuine append treats them as ordinary history.

**Coherence is enforced by construction and re-checked independently.** Every row id is read back out of the month the seed just wrote rather than invented, and the test then verifies: the actor is in the roster with a matching role; the month folder is the seeded one or null; anything matching the xray-id pattern in `target` or any detail value is a real population row; row-level entries name a drawn row assigned to whoever the entry holds responsible; every `answer-submitted` has a matching submitted `ItemAnswer` on disk for that actor and row; and every username-bearing detail names a real account. Deleted subjects (`user-deleted`, `template-deleted`) are enumerated and asserted ABSENT, so the roster check cannot quietly permit them.

`simModePlugin`'s production-leak guard was widened to `sim(Mode|Workspace|ActionLog)\.ts` so the new module is covered by the same fail-closed assertion.

**File:** `.gitignore`

**File:** `docs/development/SIMULATED_WORKSPACE.md`

**File:** `eslint.config.js`

**File:** `package-lock.json`

**File:** `package.json`

**File:** `src/dev/simModePlugin.ts`

**File:** `src/dev/simWorkspace.test.ts`

**File:** `src/dev/simWorkspace.ts`

**File:** `tsconfig.json`

**File:** `.github/workflows/e2e.yml (new)`

**File:** `docs/development/E2E_TESTS.md (new)`

**File:** `e2e/action-log.spec.ts (new)`

**File:** `e2e/adhoc-import.spec.ts (new)`

**File:** `e2e/answer-on-behalf.spec.ts (new)`

**File:** `e2e/archive.spec.ts (new)`

**File:** `e2e/boot-auth.spec.ts (new)`

**File:** `e2e/employee-queue.spec.ts (new)`

**File:** `e2e/employee-workspace.spec.ts (new)`

**File:** `e2e/helpers/app.ts (new)`

**File:** `e2e/helpers/sections.ts (new)`

**File:** `e2e/helpers/seed.ts (new)`

**File:** `e2e/notifications.spec.ts (new)`

**File:** `e2e/population-browse.spec.ts (new)`

**File:** `e2e/population-process.spec.ts (new)`

**File:** `e2e/reassignment.spec.ts (new)`

**File:** `e2e/reports.spec.ts (new)`

**File:** `e2e/settings.spec.ts (new)`

**File:** `e2e/user-management.spec.ts (new)`

**File:** `playwright.config.ts (new)`

**File:** `src/dev/simActionLog.ts (new)`

**File:** `tsconfig.e2e.json (new)`

**Before:**
```ts
// paste the replaced snippet
```

**After:**
```ts
// paste the replacing snippet
```

**Verification:** `npm run typecheck` clean; `npx vitest run src/dev/ src/data/audit/` → 10 files / 63 tests passed, including 4 new (spread and counts, date-range coverage, the coherence cross-check, and cross-build determinism covering ids and timestamps); `npx eslint` clean on touched files.

Note: a side effect of reading assignments back during seeding is that `distribution.current.json` is now derived and persisted in the sim tree — a rebuildable cache that was not there before. Full workspace build is still ~1.3 s.

The Playwright devDependency lands in `package.json` with this commit as a by-product of concurrent work; the E2E suite itself, its config and its CI workflow are NOT in this commit and follow separately once the suite has actually been run to green.

**Lines:** 31 files, +3492 / -15

---

## v112.0 — 2026-08-22 — Add (audit): log every activity with filters; delete dead v1 mapping module; correct the bundle-reserve note

**Why:** <what forced the change, and what was rejected>

**What changed:** <full description>

**Migration/rollback:** <if the change touches data formats or release mechanics>

**File:** `dist/index.html`

**File:** `scripts/check-bundle-size.mjs`

**File:** `src/components/Sidebar/Tabs/AdhocImport/index.tsx`

**File:** `src/components/Sidebar/Tabs/Archive/index.test.tsx`

**File:** `src/components/Sidebar/Tabs/Archive/index.tsx`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/NotificationManager/index.tsx`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayInspectionResults.tsx`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals.answerOnBehalf.test.tsx`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals.tsx`

**File:** `src/components/Sidebar/Tabs/Population/index.tsx`

**File:** `src/components/Sidebar/Tabs/Population/useDistributionActions.ts`

**File:** `src/components/Sidebar/Tabs/Reports/TabView.tsx`

**File:** `src/components/Sidebar/Tabs/Settings/AdminAccountSection.tsx`

**File:** `src/components/Sidebar/Tabs/Settings/SyncIntervalSection.tsx`

**File:** `src/components/Sidebar/Tabs/Settings/index.tsx`

**File:** `src/components/Sidebar/Tabs/TemplateBuilder/TabView.tsx`

**File:** `src/components/Sidebar/Tabs/UserManagement/AuditSections.tsx`

**File:** `src/components/Sidebar/Tabs/UserManagement/TabView.tsx`

**File:** `src/components/Sidebar/Tabs/UserManagement/UserManagement.css`

**File:** `src/data/adhocImport/adhocDistributionBridge.test.ts`

**File:** `src/data/adhocImport/adhocDistributionBridge.ts`

**File:** `src/data/adhocImport/adhocFieldCatalog.ts`

**File:** `src/data/adhocImport/adhocImportEmployeeView.ts`

**File:** `src/data/adhocImport/adhocImportMapping.test.ts`

**File:** `src/data/adhocImport/adhocImportMapping.ts`

**File:** `src/data/adhocImport/adhocRowProjection.test.ts`

**File:** `src/data/adhocImport/adhocSourceTable.ts`

**File:** `src/data/audit/actionLog.ts`

**File:** `src/data/labels/labelsStore.ts`

**File:** `src/data/samples/sampleMirrorStorage.ts`

**File:** `src/components/Sidebar/Tabs/UserManagement/AuditSections.filters.test.tsx (new)`

**File:** `src/components/Sidebar/Tabs/UserManagement/actionCatalog.test.ts (new)`

**File:** `src/components/Sidebar/Tabs/UserManagement/actionCatalog.ts (new)`

**File:** `src/data/audit/actionLogCoverage.test.ts (new)`

**Before:**
```ts
// paste the replaced snippet
```

**After:**
```ts
// paste the replacing snippet
```

**Verification:** `npm run lint` clean; `npm run typecheck` clean; `npm run test:run` → **391 files / 3682 tests passed, 0 failed** (was 389/3645 at v111.0.0: +37 new, none broken); `check:complexity` clean (`XrayReferrals` 1417 → 1422, `PopulationTab` 1400 → 1401, both against 1450 — kept low by putting the answer/reopen logging inside the already-module-level handler factories); `check:hex-literals` clean; `check:release` consistent at v112.0.0; `npm run build` clean; `check:bundle-size` 3826.0 kB / 1244.0 kB gzip against the raised budget.

The on-behalf log assertion was mutation-checked (stubbing the `recordAction` call makes the test fail), so it is not vacuous.

**Not verified in a real browser.** The filter bar is jsdom-tested only. `?sim=1` now makes a browser pass possible and it would be cheap here — worth doing before this is trusted.

**Deliberately left unlogged**, each with a reason rather than by omission: Report Designer designs (presentation, not governance — the closest call; templates are logged because they determine what reviewers are ASKED, a design only changes how output looks); notification acknowledgements (the data already exists in a purpose-built place with its own viewer); feedback submissions; per-user view preferences; the ad-hoc exclusion toggle and re-saves after the first (`adhoc-import-created` and `adhoc-rows-assigned` bracket the meaningful outcome); automatic daily backups (machine-initiated, no actor — the manual path IS logged); and `saveEmployeeAnswers`, which is only reachable from demo seeding.

**Migration/rollback:** additive. New action types are new enum members with optional details; existing entries are untouched and old logs still read. Rollback is a plain revert, though a reverted build would show unknown action types as their raw ids.

**Lines:** 34 files, +2354 / -879 · whole repo now 358268 lines (excl. docs/edit logs/)

---

## v111.0 — 2026-08-22 — Fix (ui): anchored popovers escape their clip; wire historical study import; open ارفاق حالات استثنائية to admin control

**Why:** <what forced the change, and what was rejected>

**What changed:** <full description>

**Migration/rollback:** <if the change touches data formats or release mechanics>

**File:** `CLAUDE.md`

**File:** `src/App.subTabFeatureGate.test.tsx`

**File:** `src/auth/permissionMatrixEffect.test.ts`

**File:** `src/auth/subTabFeatureGate.ts`

**File:** `src/auth/tabCatalog.test.ts`

**File:** `src/auth/tabCatalog.ts`

**File:** `src/auth/userManagement.ts`

**File:** `src/components/DataTable/index.tsx`

**File:** `src/components/GlobalMonthSelector/GlobalMonthSelector.css`

**File:** `src/components/GlobalMonthSelector/GlobalMonthSelector.test.tsx`

**File:** `src/components/GlobalMonthSelector/GlobalMonthSelector.tsx`

**File:** `src/components/Sidebar/Tabs/AdhocImport/AdhocImport.css`

**File:** `src/components/Sidebar/Tabs/AdhocImport/index.test.tsx`

**File:** `src/components/Sidebar/Tabs/AdhocImport/index.tsx`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals/caseFilter.ts`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals/subComponents.tsx`

**File:** `src/components/Sidebar/Tabs/Population/BrowseDataView.filter.test.tsx`

**File:** `src/components/Sidebar/Tabs/Population/BrowseDataView.tsx`

**File:** `src/components/Sidebar/Tabs/Population/BrowseDataView.workerRace.test.tsx`

**File:** `src/components/Sidebar/Tabs/Population/Population.adhocSubTab.test.tsx`

**File:** `src/components/Sidebar/Tabs/Population/Population.css`

**File:** `src/components/Sidebar/Tabs/Population/index.tsx`

**File:** `src/components/Sidebar/Tabs/UserManagement/PermissionSections.test.tsx`

**File:** `src/data/adhocImport/adhocRecordMigration.test.ts`

**File:** `src/data/adhocImport/adhocRecordMigration.ts`

**File:** `src/data/labels/labelsStore.ts`

**File:** `src/styles/primitives.css`

**File:** `src/components/DataTable/popoverPlacement.test.tsx (new)`

**File:** `src/components/Popover/AnchoredPopover.css (new)`

**File:** `src/components/Popover/AnchoredPopover.test.tsx (new)`

**File:** `src/components/Popover/AnchoredPopover.tsx (new)`

**File:** `src/components/Popover/anchoredPosition.test.ts (new)`

**File:** `src/components/Popover/anchoredPosition.ts (new)`

**File:** `src/components/Sidebar/Tabs/AdhocImport/HistoricalPanel.tsx (new)`

**File:** `src/components/Sidebar/Tabs/AdhocImport/TemplateMappingPanel.tsx (new)`

**File:** `src/components/Sidebar/Tabs/AdhocImport/historicalImport.test.tsx (new)`

**Before:**
```ts
// paste the replaced snippet
```

**After:**
```ts
// paste the replacing snippet
```

**Verification:** `npm run lint` clean; `npm run typecheck` clean; `npm run test:run` → **389 files / 3645 tests passed, 0 failed** (was 385/3600 at v110.0.0: +45 new, none broken); `check:complexity` clean (`AdhocImportTab` 675 → 921 against the 1450 ceiling; the two largest functions untouched); `check:hex-literals` clean; `check:release` consistent at v111.0.0; `npm run build` clean; `check:bundle-size` within budget.

**Popover geometry verified in a real browser** via `?sim=1` — the first change to actually use the simulated workspace for the thing jsdom structurally cannot do. jsdom performs no layout, so every collision assertion in the unit tests is driven by a stubbed `getBoundingClientRect` and reads back the decision, never a measured pixel. In Chromium at 1280×800: five filter menus opened across the queue table, **all five fully inside the viewport**, and the open popover's parent chain measured as `body → html` with **no clipping ancestor** — proving both the clamp and the portal, neither of which a jsdom test can establish.

**Still not verified:** the clamped `max-height` scroller's feel on a real multiselect, z-order against the deck customizer overlay, and the ≤640px mobile-sidebar case for the month popover.

**Found and deliberately left:** three `position: fixed` toasts that are not portalled (`.rh-toast`, `.ntf-toast`, `.ew-approval-toast`) and so likely anchor to their tab's box rather than the viewport; dead CSS (`.ref-filter-menu`, `.ew-col-panel`, `.arc-modal-backdrop`); and `FieldDropDialog`'s `Math.min`-only clamp. None are "popups or filters" in the reported sense.

**Migration/rollback:** additive. No id, key or stored-file shape changed, so existing workspaces keep their permission rows and ad-hoc records. A ceiling is consulted at render time and never persisted. Rollback is a plain revert.

**Lines:** 36 files, +2773 / -160 · whole repo now 355778 lines (excl. docs/edit logs/)

---

## v110.0 — 2026-08-22 — Add (dev): writable simulated workspace for browser automation; repo automation and ecc cleanup

**Why:** <what forced the change, and what was rejected>

**What changed:** <full description>

**Migration/rollback:** <if the change touches data formats or release mechanics>

**File:** `.claude/ecc-tools.json`

**File:** `.claude/identity.json`

**File:** `dist/index.html`

**File:** `src/auth/AuthGate.tsx`

**File:** `src/data/workspace/WorkspaceProvider.tsx`

**File:** `src/data/workspace/demoWorkspace.ts`

**File:** `vite.config.ts`

**File:** `.github/codeql/codeql-config.yml (new)`

**File:** `.github/dependabot.yml (new)`

**File:** `.github/scripts/pr-report-metrics.mjs (new)`

**File:** `.github/scripts/pr-report-render.mjs (new)`

**File:** `.github/workflows/codeql.yml (new)`

**File:** `.github/workflows/pr-report.yml (new)`

**File:** `docs/development/SIMULATED_WORKSPACE.md (new)`

**File:** `src/data/workspace/demoWorkspace.test.ts (new)`

**File:** `src/dev/simMode.prod.ts (new)`

**File:** `src/dev/simMode.test.ts (new)`

**File:** `src/dev/simMode.ts (new)`

**File:** `src/dev/simModePlugin.ts (new)`

**File:** `src/dev/simMount.test.tsx (new)`

**File:** `src/dev/simWorkspace.test.ts (new)`

**File:** `src/dev/simWorkspace.ts (new)`

**Before:**
```ts
// paste the replaced snippet
```

**After:**
```ts
// paste the replacing snippet
```

**Verification:** `npm run lint` clean; `npm run typecheck` clean; `npm run test:run` → **385 files / 3600 tests passed, 0 failed** (was 381/3576 at v109.0.0: +24 new, none broken); `check:complexity`, `check:hex-literals`, `check:vendor` clean; `check:release` consistent at v110.0.0; `npm run build` clean; `check:bundle-size` 3772.8 kB / 1228.3 kB gzip.

**Verified in a real browser — the first time this repo has managed it.** Chromium via Playwright against `?sim=1`:
- `?sim=1&role=supervisor` lands straight in the app: no picker, no login form, simulation banner present.
- The employee picker rendered `الكل — جميع الموظفين (96)` plus 34/29/19/14 per employee — **summing exactly to 96**, matching the seed, with the supervisor's own row marked `أنا`.
- All three case-filter chips present; `مستهدف المؤشر` narrowed the table and reported a non-zero count, confirming affirmative risk rows survive the draw into the sample.
- `?sim=1&role=employee` showed that employee's 34 rows; opening one gave **22 editable controls**, and a typed value persisted — proving the workspace is genuinely writable rather than the read-only demo.
- Zero uncaught page errors and zero console errors throughout.

**Bundle headroom is now 35.8 kB raw (0.9%).** The next feature of any size will hit the ceiling; the honest fix is deleting the orphaned v1 ad-hoc modules, already noted in `check-bundle-size.mjs`, rather than raising the number a fourth time.

**Migration/rollback:** additive and dev-only. Nothing in the production bundle changes behaviour; the new workflows are inert until a PR runs. Rollback is a plain revert.

**Lines:** 22 files, +2906 / -930 · whole repo now 352976 lines (excl. docs/edit logs/)

---

## v109.0 — 2026-08-22 — Add (employee queue): employee picker, answer-on-behalf permission, individual reassign

**Why:** <what forced the change, and what was rejected>

**What changed:** <full description>

**Migration/rollback:** <if the change touches data formats or release mechanics>

**File:** `docs/product/PRODUCT_SPECIFICATION.md`

**File:** `src/auth/userManagement.test.ts`

**File:** `src/auth/userManagement.ts`

**File:** `src/components/InspectionPanel/index.tsx`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals.caseFilter.test.tsx`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals.queuePresentation.test.tsx`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals.test.tsx`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals.tsx`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals/XrayReferrals.css`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals/subComponents.tsx`

**File:** `src/data/answers/answerStorage.ts`

**File:** `src/data/answers/answerTypes.ts`

**File:** `src/data/answers/reopenAnswer.ts`

**File:** `src/data/labels/labelsStore.ts`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals.answerOnBehalf.test.tsx (new)`

**File:** `src/data/answers/answerOnBehalf.test.ts (new)`

**Before:**
```ts
// paste the replaced snippet
```

**After:**
```ts
// paste the replacing snippet
```

**Verification:** `npm run lint` clean; `npm run typecheck` clean; `npm run test:run` → **381 files / 3576 tests passed, 0 failed** (was 379/3542 at v108.1.0: +34 new, none broken); `npm run check:complexity` clean — `XrayReferrals` came DOWN from 1449 to **1417/1450** despite the additions, via three handler extractions to module scope; `npm run check:hex-literals` clean; `npm run check:release` consistent at v109.0.0; `npm run build` clean; `npm run check:bundle-size` within budget.

**Not verified in a real browser.** Same `showDirectoryPicker()` gate as the previous entries — the picker, the on-behalf lock and the panel's reassign button are covered by jsdom only. A human pass over "pick an employee → answer one of their unanswered rows → confirm the history records who answered → reassign a single row" is still owed.

**Migration/rollback:** additive. `answeredOnBehalfBy` is optional and absent means the assignee answered themselves; `normalizeUserManagementState` backfills the new permission as disabled for existing workspace files. Rollback is a plain revert — no data written by this version becomes unreadable by the previous one, though an older build would ignore the new field.

**Known gaps, none blocking.** The history is durable on disk but nothing renders it: `answerFieldRows`, the executive/KPI joins and the Power BI export still attribute work to `answeredBy` (the assignee), which is correct for workload attribution but leaves "who really answered" invisible in reporting. And `view-all-entries` is a de facto prerequisite — granting only `answer-on-behalf` yields a page with no other employee's rows on it.

**Lines:** 16 files, +1917 / -216 · whole repo now 352182 lines (excl. docs/edit logs/)

---

## v108.1 — 2026-08-22 — Add (employee queue): three case filters — جميع الحالات / مستهدف المؤشر / إحالات استثنائية

**Why:** Owner request: an employee working the queue needs to reach the two case groups that matter without hunting through column filters — «so it's easy to identify and reach them». `مستهدف المؤشر` is the risk-engine-flagged set; `إحالات استثنائية` is confirmed (owner) to mean ad-hoc imported cases, i.e. rows assigned outside the regular monthly pipeline.

**What changed:** A three-chip segmented control above the queue in `XrayReferrals`, with a live count on each chip — the count is the point, since it lets a reviewer see how many rows a group holds before clicking into it. Predicates: `all` → identity; `risk-targeted` → `engineVerdictOf(entry.row.targetedByRiskEngine) === "اشتباه"`; `adhoc` → `isAdhocEntry(entry)`. All three run over the `showMyOnly`-scoped set and the counts are computed over that same set, so a chip reading *7* opens onto exactly 7 rows.

The buckets deliberately do **not** partition: an ad-hoc row whose risk column says «نعم» is counted by both chips, so they don't sum to `all`. A test asserts that so nobody later "fixes" it into a partition.

No extra disk read — `targetedByRiskEngine` is already in `EMPLOYEE_MIRROR_STUB_FIELDS`, so the queue's inline row stub carries it.

**Vocabulary extracted rather than duplicated.** `engineVerdictOf` and its AFFIRMATIVE/NEGATIVE sets moved from `reporting/executive/deck2/section3/riskEngineAgreement.ts` into a new `data/population/riskEngineVerdict.ts`; the reporting module imports and re-exports it. Importing an executive-deck section into an EmployeeWorkspace view would have dragged that whole section into the view's chunk, and re-typing the word list in the view would have let the two drift — now widening the vocabulary fixes the deck and the chip in one edit. The move is pure: the section-3 suite (223 tests) passed unchanged, which is the check that proves it.

`resetToken` now includes the filter, because changing the visible row set outright makes page 4 of a one-page result a dead end. It cannot bounce a reviewer off an open row — `resetToken` only feeds DataTable's paging state, never `selEntryId` or the panel.

A chip that excludes the currently-open row is the same event shape as a supervisor reassigning it mid-edit, so the existing render-derived `dirtyEntryId`/`lastPanelEntry` retention absorbs it: the panel keeps the row and the typed answers. A test types into the panel, switches to a chip that excludes that row, and asserts the draft survives — this file has a documented regression where re-pointing the panel destroyed unsaved work, so it is verified rather than assumed.

`QueueScopeSwitcher` was extracted verbatim to `subComponents.tsx` to buy back budget: `XrayReferrals` was at 1443/1450 on `max-lines-per-function` and is now 1449. **The next change in that file needs another factor-out before it will fit.**

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals.tsx`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals/XrayReferrals.css`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals/subComponents.tsx`

**File:** `src/data/labels/labelsStore.ts`

**File:** `src/data/reporting/executive/deck2/section3/riskEngineAgreement.ts`

**File:** `src/data/reporting/executiveReportTypes.ts`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals.caseFilter.test.tsx (new)`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals/caseFilter.test.ts (new)`

**File:** `src/components/Sidebar/Tabs/EmployeeWorkspace/views/XrayReferrals/caseFilter.ts (new)`

**File:** `src/data/population/riskEngineVerdict.test.ts (new)`

**File:** `src/data/population/riskEngineVerdict.ts (new)`

**Before:**
```ts
// paste the replaced snippet
```

**After:**
```ts
// paste the replacing snippet
```

**Verification:** `npm run lint` clean; `npm run typecheck` clean; `npm run test:run` → **379 files / 3542 tests passed, 0 failed** (was 376/3514 at v108.0.0: +28 new, no existing test touched or broken); `npm run check:complexity` clean; `npm run check:hex-literals` clean; `npm run check:release` consistent at v108.1.0; `npm run build` clean; `npm run check:bundle-size` within budget.

**Coverage caveat, not a defect — the owner needs this before trusting the chip.** The recognised vocabulary is `نعم / مستهدف / y / yes / true / 1` (affirmative) and `لا / غير مستهدف / n / no / false / 0` (negative), trimmed and lower-cased; everything else, **including every blank**, is "unknown" and excluded. That set was guessed at design time — `DATA_DICTIONARY.md` row 27 carries no notes for this column, and `riskDataColumns.ts` only ever knew the header aliases, never the cell values. If a real risk file writes `مستهدف بالكامل`, `Y - مستهدف`, `✔`, or leaves the column mostly empty, the chip reads 0 or far too low. It fails in the safe direction — it under-counts and never presents an unflagged case as engine-flagged — but the first real month this ships against should be checked against the executive deck's risk-engine coverage block, which prints the recognised/unrecognised/blank split. Widening the sets in `riskEngineVerdict.ts` then fixes both surfaces at once.

**Lines:** 11 files, +1072 / -55

---
