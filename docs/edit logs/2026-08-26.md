# Edit Log — 2026-08-26

## v125.3 — 2026-08-26 — Remove (auth): the login form's redundant demo/demo branch — the picker's demo popup is the one entry

**Why:** Owner follow-up on v125.2: with the demo popup sitting on the
address-picker screen — reachable BEFORE any workspace is chosen — the second
demo/demo branch in the login form was redundant (that form only appears after
a workspace is mounted, and its demo branch silently swapped the mounted
workspace for the demo one). One entry point, one behavior.

**What changed:** `loginAsEmployee`'s demo/demo branch was removed; the login
form now only authenticates against the mounted workspace's own accounts, with
a NOTE pointing at WorkspaceGate's popup as the single demo entry. The now
unused `DEMO_PASSWORD` import and `enterDemoWorkspace` destructure were
dropped from AuthGate (the auto-login effect and its `DEMO_USERNAME` stay —
that's what issues the session once the picker mounts the demo workspace).
`authConfig.ts`'s block comment and `SECURITY_MODEL.md`'s TEC-06 note updated
to match.

**Before:** (AuthGate.tsx, loginAsEmployee)
```ts
if (normalizedInput === DEMO_USERNAME && password === DEMO_PASSWORD) {
  isDemoSessionRef.current = true;
  // …mounts the demo workspace from the login form
}
```

**After:**
```ts
// NOTE: the demo has exactly ONE entry point — WorkspaceGate's «الدخول
// بنمط التجربة» popup on the address-picker screen, BEFORE any workspace
// is chosen. The login form only ever authenticates against the mounted
// workspace's own accounts.
```

**Verification:** `npm run lint` ✅ clean · `npm run typecheck` ✅ clean ·
`npm run test:run` ✅ 425 files / 4025 tests passed · `npm run build` ✅

**Lines:** 3 files, +22 / -36

---

## v125.2 — 2026-08-26 — Change (demo): picker demo entry becomes «الدخول بنمط التجربة» with a password-only popup

**Why:** Owner follow-up on v125.1's picker entry: the start screen should offer
«اختيار مجلد» or a demo entry named like «الدخول بنمط التجربة» whose popup asks
for ONE password — demo — not a username+password pair. Two fields were
friction for a fixed, published credential (the username was always "demo"
anyway).

**What changed:** The picker's demo dialog dropped its username field —
`submitDemoLogin` now validates the password alone (trimmed, case-insensitive)
against `DEMO_PASSWORD`; the button and dialog were renamed to «الدخول بنمط
التجربة» and the description/first-run hint/error labels reworded to the
password-only flow. The ordinary login form still accepts the full demo/demo
pair (unchanged), and `SECURITY_MODEL.md`'s TEC-06 note records the split.
The unused `wsgate_demo_username_label` label key was removed.

**File:** `dist/index.html`

**File:** `docs/architecture/SECURITY_MODEL.md`

**File:** `src/data/labels/labelsStore.ts`

**File:** `src/data/workspace/WorkspaceGate.tsx`

**Before:** (WorkspaceGate.tsx)
```ts
if (
  demoUsername.trim().toLowerCase() === DEMO_USERNAME &&
  demoPassword === DEMO_PASSWORD
) {
```

**After:**
```ts
if (demoPassword.trim().toLowerCase() === DEMO_PASSWORD) {
```

**Verification:** `npm run lint` ✅ clean · `npm run typecheck` ✅ clean ·
`npm run test:run` ✅ 425 files / 4025 tests passed · live Chromium walkthrough
of the built `dist/index.html`: «الدخول بنمط التجربة» → password `demo` →
demo app mounts with the seeded month.

**Lines:** 4 files, +18 / -37

---

## v125.1 — 2026-08-26 — Add (auth/demo): one writable demo mode behind demo/demo — merged viewer entry, role-preview walkthrough, ~100-image seeded sample

**Why:** The app had two disconnected "try it" mechanisms: the hidden viewer/view
passcode on the workspace picker (Alt+A ثم T) that mounted a READ-ONLY demo, and
the dev-only `?sim=1` simulated workspace (excluded from production builds).
The owner asked for one user-facing demo: sign in with **demo / demo**, get a
fully working sandbox with realistic data (~100-image sample) that needs no
workspace folder and no File System Access API, and be able to showcase
per-role flows — e.g. open الموظف view, see the demo account's own assigned
queue (distinct from the supervisor's), and actually reassign a sample — which
the read-only viewer could never do. A separate Demo-1..5 account switcher was
considered and explicitly rejected by the owner mid-review ("keep it as it
is") — the existing role-preview switch is the walkthrough mechanism.

**What changed:**

- `authConfig.ts` — `VIEWER_USERNAME`/`VIEWER_PASSWORD` ("viewer"/"view")
  replaced by `DEMO_USERNAME`/`DEMO_PASSWORD` ("demo"/"demo").
- `WorkspaceGate.tsx` — the hidden passcode modal became a visible «الدخول
  التجريبي» button (the Alt+A ثم T shortcut still opens it) with
  username/password fields validating demo/demo. Also offered on the
  unsupported-browser screen: the demo runs entirely in memory, so it is the
  one thing a browser without the File System Access API can run.
- `AuthGate.tsx` — the ordinary login form accepts demo/demo too (switches to
  the in-memory demo workspace; the existing auto-login effect then issues the
  session, now under username "demo"). `SyncTick` and `WorkspaceErrorSink` are
  no longer disabled for demo sessions.
- **Writable demo:** `usePermissions` no longer forces read-only for
  `mode: "demo"`, and `enterDemoWorkspace` no longer flips the storage-level
  read-only flag — every mutation runs for real against the in-memory tree and
  evaporates on logout. The session still never persists (authSession's
  demo-mode guard unchanged).
- `AdminToolbar.tsx` — demo sessions keep the ordinary role-preview switch
  (الإدارة/المدير/المشرف/الموظف/ضيف) and the refresh button instead of the old
  frozen "view mode" badge; only the admin-feedback button stays
  real-admin-only. `Sidebar.tsx` — month creation no longer demo-gated.
- `demoWorkspace.ts` — seed grown to 5 ports / 400 rows with a 25% stage-1
  rule → exactly 100 sampled images; roster gains a `demo` employee account
  («الحساب التجريبي») and the allocations give it the largest queue share
  (35%) with a distinct slice for the supervisor (malrogi, 10%), so
  employee-vs-supervisor queues visibly differ and reassignment has both sides
  populated. `WorkspaceSeedProfile` gains an optional `employees` roster
  (attached at call time — a module-scope roster build broke tests that mock
  `auth/userManagement`); the dev sim profile is untouched.
- `labelsStore.ts` — demo labels rewritten for the writable sandbox (banner,
  toolbar badge, picker dialog, first-run hint now says demo/demo).

**Migration/rollback:** No data-format change; the demo workspace remains
memory-only. Reverting the commit restores the read-only viewer behavior.

**File:** `dist/index.html`

**File:** `src/auth/AdminToolbar.test.tsx`

**File:** `src/auth/AdminToolbar.tsx`

**File:** `src/auth/AuthGate.test.tsx`

**File:** `src/auth/AuthGate.tsx`

**File:** `src/auth/authConfig.ts`

**File:** `src/auth/usePermissions.ts`

**File:** `src/components/Sidebar/Sidebar.tsx`

**File:** `src/data/labels/labelsStore.ts`

**File:** `src/data/workspace/WorkspaceGate.tsx`

**File:** `src/data/workspace/WorkspaceProvider.tsx`

**File:** `src/data/workspace/demoWorkspace.test.ts`

**File:** `src/data/workspace/demoWorkspace.ts`

**Before:** (usePermissions.ts — the one line that froze every demo mutation)
```ts
isReadOnly: session.mode === "demo" || isReadOnlyMode(),
```

**After:**
```ts
// Demo sessions are WRITABLE (2026-08-26): every mutation runs for real
// against the in-memory demo workspace, so mode "demo" no longer forces
// read-only.
isReadOnly: isReadOnlyMode(),
```

**Verification:** `npm run lint` ✅ clean · `npm run typecheck` ✅ clean ·
`npm run test:run` ✅ 425 files / 4025 tests passed · `npm run build` ✅ ·
live Chromium walkthrough of the built `dist/index.html`: demo/demo sign-in from
the picker button, 400-row population + 100-image sample loaded, role preview →
الموظف shows the demo account's own queue (35 assigned: 14 مكتملة / 21 لم تبدأ)
with the assignment controls enabled.

**Lines:** 13 files, +994 / -814

---

## v125.0 — 2026-08-26 — Fix (executive-report/deck3): rebuild to the handoff design and restore the deck2 viewer chrome

**Why:** The v124 deck3 (PR #105) shipped as a loose approximation of the owner's
high-fidelity handoff (`docs/design/design_handoff_xray_qa_deck/`), and the owner
flagged both drifts explicitly: (1) the export had none of deck2's viewer chrome —
no side section nav, no header toolbar, no طباعة / PDF button, no fullscreen — even
though the spec said viewer mechanics reuse deck2's pattern; (2) the slide design
didn't match the handoff (rounded cards everywhere though the handoff forbids
border radius, a 2×2-cards matrix instead of the 4×4 grid, no cover logo/org
block, no ghost-numeral dividers, several slides showing the same rows in both
"land" and "sea" panels). Two of the drifts were outright bugs: divider slides
rendered white-ish text with **no dark background** (nearly invisible), and the
responsive scale rule `transform:scale(calc((100vw - 32px) / 1920))` is invalid
CSS (length where scale() needs a number), so slides never scaled down at all.

**What changed:** Full rebuild of the deck3 module to the handoff's exact
layouts, plus deck2 chrome parity:

- `theme.ts` — rewritten to the handoff tokens verbatim: no border radius
  (except the 16px 50% dots), tinted land/sea panels with 5px top rules and
  colored dot titles, dark `#10304f` dividers with the 280px ghost numeral,
  2px-navy table/section rules, the contents grid (`120px 1fr 420px 130px`),
  KPI bands, the 4×4 matrix grid, engine-agreement band, impact callouts with
  the 5px inline-start bar, and the handoff chart geometry. Also carries the
  deck3-skinned viewer chrome CSS (side nav, sticky toolbar, single-slide
  fullscreen, print = one 1920×1080 page per slide) and the `--v3-scale`
  variable that replaces the invalid pure-CSS scale calc.
- `index.ts` — `buildDeckV3Html` now emits the same chrome markup as
  `buildDeckV2Html` (nav + progress, toolbar with fullscreen + طباعة / PDF,
  slide prev/next + counter) and reuses deck2's `DECK_NAV_SCRIPT` /
  `DECK_FULLSCREEN_SCRIPT` verbatim, plus a new `DECK_V3_SCALE_SCRIPT` that
  owns the 1920×1080 fit ratio (viewport estimate before first paint, precise
  measure after DOM ready, both-axes fit in fullscreen).
- `slideKit.ts` / `chartKit.ts` — templates and chart primitives rebuilt to the
  handoff structures (slide shells with `data-section` hooks for the nav
  script, zero-padded `NN / 21` footers only on content slides, grouped
  glossary grids, level columns with color chips, tinted plots with capped
  centered bars, reference lines with edge labels, fixed-width centered groups
  for the sea chart, honest null-value gaps instead of 0% bars).
- `slides.ts` — all 21 slides rebuilt per the handoff and wired to real data:
  cover/closing with the ZATCA logo + organization lines; per-level population
  table with share bars and a data-derived حصر كامل/عدد ثابت column; port
  panels with per-type coverage, `population (sample)` cells and totals rows;
  the three-tile accuracy band plus a per-risk-level accuracy table; per-port
  accuracy panels with the three accuracy columns and bracketed counts; land/
  sea per-port charts with pooled type averages; per-level clean/suspicion
  charts; the full confusion matrix with row/column totals; L1-vs-L2 pooled
  five-row panels and per-port grouped charts; security-team agreement from
  `crossTeamMatrix` plus the risk-engine band (targeted → agree/disagree →
  reviewer-upheld split, via `engineVerdictOf`); marking/quality impact split
  with pinned callouts. All data-derived strings are escaped.
- `model/reportModel.ts` — exposes `stageAccuracy` (the existing
  `aggregates.byStage` fold) so the per-risk-level accuracy slides read the
  shared fold instead of re-tallying.
- `deck2/index.ts` — the two chrome scripts gain `export` (no behavior change;
  deck2 output byte-identical).
- Level fixed-count shares (2,500/1,875/1,875 in the handoff) derive from the
  real `monthlyTarget` (40/30/30%), never the placeholder 6,250.

**Migration/rollback:** No data-format change. `ReportModel.stageAccuracy` is
additive. Rolling back is reverting the commit; the «التصميم الجديد» toggle and
`deckEditionPreference` storage are untouched.

**File:** `dist/index.html`

**File:** `src/data/reporting/executive/deck2/index.ts`

**File:** `src/data/reporting/executive/deck3/chartKit.test.ts`

**File:** `src/data/reporting/executive/deck3/chartKit.ts`

**File:** `src/data/reporting/executive/deck3/deck3.test.ts`

**File:** `src/data/reporting/executive/deck3/index.ts`

**File:** `src/data/reporting/executive/deck3/slideKit.ts`

**File:** `src/data/reporting/executive/deck3/slides.ts`

**File:** `src/data/reporting/executive/deck3/theme.ts`

**File:** `src/data/reporting/executive/model/reportModel.ts`

**Before:** (deck3/theme.ts — dividers had light-page background with white text,
and the scale rule was invalid CSS, so it was silently dropped)
```ts
.v3-divider-num{ font-size:32px; font-weight:700; color:var(--v3-gold-lighter); }
/* no background rule for .v3-divider anywhere — text was invisible */
@media screen and (max-width:1980px){
  .slide.v3{ transform:scale(calc((100vw - 32px) / 1920)); /* length, not number — invalid */ }
}
```

**After:** (deck3/theme.ts)
```ts
.slide.v3.v3-divider{background:var(--v3-navy);color:var(--v3-bg);justify-content:space-between;padding:96px 100px 72px;border:none;}
.slide.v3{ /* … */ transform:scale(var(--v3-scale)); /* set by DECK_V3_SCALE_SCRIPT */ }
```

**Verification:** `npm run lint` ✅ clean · `npm run typecheck` ✅ clean ·
`npm run test:run` ✅ 425 files / 4024 tests passed ·
`npm run check:complexity` ✅ · `npm run check:hex-literals` ✅ no swept file
exceeds its baseline · `npm run check:release` ✅ v125.0 ↔ 125.0.0 ·
`npm run check:vendor` ✅ checksum verified · `npm run build` ✅ ·
`npm run check:bundle-size` ✅ 3920.7 kB raw / 1265.8 kB gzip (ceiling 30 MB / 10 MB)

**Lines:** 10 files, +3007 / -1495 · whole repo now 398011 lines (excl. docs/edit logs/)

---
