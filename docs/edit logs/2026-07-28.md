# Edit log — 2026-07-28

Complete XQAP codebase changes recorded on this date.

---

## v59.70 — 2026-07-28 — Fix (executive-deck): source-agreement chart — 2×3 levels-vs-teams grid replaces the unreadable 6×6 numeric-axis heatmap

Owner feedback after reviewing the rendered `slide-s3-source-agreement` page
(default variant, "1/4"): "the graph on top right is suppose to be level 1
and 2 on y axis and other temas on x axis on top. 123456 mean nothing and
current page is hard to read." The shipped chart was a 6×6 lower-triangle
heatmap of all `C(6,2)=15` source pairs, with column headers reduced to bare
numbers `1..6` because 6 full Arabic source names collided across the
chart's top edge — and with typical monthly data, only 3 of the 15 cells
were ever populated, so the chart mostly read as empty dashes behind a
meaningless numeric axis.

Full design spec (brainstormed and approved with the owner before
implementation, one clarifying question at a time): `docs/superpowers/specs/
2026-07-28-deck2-source-agreement-levels-vs-teams-design.md`. Implementation
plan (executed via subagent-driven-development, one fresh implementer +
reviewer pair per task, in an isolated worktree/branch):
`docs/superpowers/plans/2026-07-28-deck2-source-agreement-levels-vs-teams.md`.

**Fix:** the chart on both the default and Grid variants now answers the
page's own stated question narrowly — how do the two X-ray inspection levels
compare against the other inspection teams — instead of every one of the 15
possible source pairs:
- 2 rows (المستوى الأول / الثاني), 3 real-name columns (التفتيش اليدوي /
  المعاكس / الوسائل الحية) — a genuine rectangle, no lower-triangle indexing,
  no numeric axis needed at only 3 columns.
- `review` is dropped from the chart (the reviewer card next to it already
  shows both levels vs. the reviewer — repeating those numbers would
  duplicate information one glance away).
- level1↔level2 agreement (previously the single highest-value cell in the
  old matrix) gets its own standalone stat callout above the grid, since it
  isn't a "level vs team" comparison.
- The sample-count (ن) companion table shrinks from a 6×6 triangle to a
  plain 2×3 table.

The Ledger variant's existing 15-row pair table and the Briefing variant's
rank list are unchanged (per the owner's explicit choice during design — the
confusing chart was the readability complaint, not those two table/list
variants). The reviewer card is unchanged in every variant. Net code effect
is a genuine simplification, not just a relabel: the 6×6 lower-triangle
indexing and its "why numbers not names" tradeoff are gone entirely from the
two rewired call sites.

Two real plan/test gaps surfaced and were fixed live during implementation,
each caught by an implementer subagent stopping instead of guessing past it:
1. The original plan told the "default variant" task to delete the old
   `buildHeatMatrix`/`comparableGrid` functions immediately — but the Grid
   variant (a separate, later task) still called both, so deleting them
   early broke typecheck. Plan corrected in place; the old functions were
   kept alive until the Grid-variant task rewired its own call site, then
   deleted together in that same task.
2. Four test assertions sliced a fixed 300-character window from a marker
   to check for percentage text, but the real markup (an inline SVG icon
   plus wrapper) runs ~395 characters — correct production code still
   failed those 4 tests. Fixed by slicing to the block's own closing
   `</div>` instead of a magic character count, which is also more robust
   to future icon/text length changes.

**File:** `src/data/reporting/executive/deck2/section3/sourceAgreement.ts`

**Before:**
```ts
function buildHeatMatrix(cells: CrossTeamMatrixCell[]): HeatMatrix {
  const index = indexPairs(cells);
  return {
    rows: SOURCE_ORDER.map((source, i) => `${SOURCE_LABELS[source]} (${i + 1})`),
    cols: SOURCE_ORDER.map((_, i) => String(i + 1)),
    values: SOURCE_ORDER.map((rowSource, ri) =>
      SOURCE_ORDER.map((colSource, ci) => {
        if (ci >= ri) return null;
        const cell = pairAt(index, colSource, rowSource);
        return cell ? gatedRate(cell.comparable, cell.agreementRate) : null;
      }),
    ),
  };
}
```

**After:**
```ts
function buildLevelsTeamsMatrix(cells: CrossTeamMatrixCell[]): HeatMatrix {
  const index = indexPairs(cells);
  return {
    rows: LEVEL_SOURCES.map((s) => SOURCE_LABELS[s]),
    cols: TEAM_SOURCES.map((s) => SOURCE_LABELS[s]),
    values: LEVEL_SOURCES.map((level) =>
      TEAM_SOURCES.map((team) => {
        const cell = pairAt(index, level, team);
        return cell ? gatedRate(cell.comparable, cell.agreementRate) : null;
      }),
    ),
  };
}
```

**File:** `src/data/reporting/executive/deck2/section3/sourceAgreement.test.ts`

**Before:** (excerpt — the pair-percentages assertion)
```ts
    const html = sourceAgreementSlide(model, 12, 24, false);
    expect(html).toContain(">75%</text>"); // L1 × L2
    expect(html).toContain(">90%</text>"); // L1 × reviewer
    expect(html).toContain(">85%</text>"); // L2 × reviewer
```

**After:** (excerpt)
```ts
    const html = sourceAgreementSlide(model, 12, 24, false);
    for (const pct of [80, 50, 25, 95, 75]) {
      expect(html).toContain(`>${pct}%</text>`);
    }
    const statStart = html.indexOf('class="s3sa-lvl-stat"');
    expect(html.slice(statStart, html.indexOf("</div>", statStart) + "</div>".length)).toContain("75.0%");
```

**Lines:** 242730 → 244096 (net +1366, incl. the design spec, implementation
plan, and this edit-log entry itself) · 2 files (the two touched source
files), +266 / -89.

156/156 test files, 1298/1298 tests passing (up from 1295 baseline).
`typecheck`/`lint`/`check:hex-literals`/`check:complexity` all clean. Live
DOM-geometry verification against the real dev-preview iframe (screenshot
compositing unavailable in this sandbox, same known limitation as prior
entries) confirmed no content clipping in either the narrow/stacked or the
real ~1120px+ side-by-side responsive layout, correct real-name column
headers with zero stray numeric axis remnants, and a correctly-populated
stat callout, on both the default and Grid variants.

---

## v59.69 — 2026-07-28 — Fix (executive-deck): slide content silently clipped at any viewport narrower than the ~1120px design width — root cause of "most pages look bad, unfitted text/bad shapes"

User reported, after visually reviewing the actual report across all 4 design
variants: "most of it look bad unfitted text or bad shapes." Live DOM-geometry
auditing (screenshot compositing isn't available in this sandbox — see prior
entries — so this used `getBoundingClientRect`/`getComputedStyle` sweeps across
every slide × every variant panel, activating each panel exactly the way
`DECK_VARIANT_SCRIPT` does) found genuine, severe content overflow on the
majority of pages, but **only at viewport widths below ~1150px** — a full-width
desktop browser (1280px+) showed none of it, which is exactly why this shipped
undetected through the whole multi-day fan-out effort: every verification pass
across every batch used a full-size dev viewport.

**Root cause:** `.slide` (`deck/deckTheme.ts`, shared by both the v1 and v2
decks) was sized `width:min(1120px,100%); aspect-ratio:297/167; overflow:hidden`
— i.e. its HEIGHT was derived from its WIDTH. Every page's row-budget math
(`BASE_ROWS_PER_PAGE`, filler-row heights, `briefingRankPlan`'s density tiers,
per-card row caps, etc., across the whole deck) is calibrated in fixed pixels
against the ~630px of height the slide gets at its full 1120px design width.
Any container narrower than ~1150px (accounting for the side-nav's reserved
space) — an ordinary laptop-width browser window, or the admin design
customizer's iframe (sized by its own modal chrome, not guaranteed to hand the
deck a full 1120px) — shrunk the slide's WIDTH via `min(...,100%)`, and
`aspect-ratio` shrunk its HEIGHT to match, silently invalidating every one of
those pixel budgets. The mismatch between "the actual room" and "the room the
layout math assumes" then clipped invisibly via the slide's own
`overflow:hidden` — exactly "unfitted text or bad shapes" from the reader's
side, with no error, no console warning, nothing to catch in a unit test that
never renders at a real narrow width.

**Fix, four parts, discovered and verified incrementally against a live,
worktree-scoped dev server sweeping viewports from 830px through 1440px:**

1. **Decouple height from width.** `.slide` now gets a FIXED `height:630px`
   instead of `aspect-ratio:297/167` — the same ~630px the design was always
   calibrated against, now guaranteed regardless of how narrow the container
   gets. This alone fixed 15 of 17 overflow cases found in the first sweep (at
   1024px viewport width). Confirmed zero regression at full design width
   (1120x630, unchanged) and at 1440px+ (unaffected, width still caps at
   1120px).
2. **Two localized `min-height:0` grid-item fixes** for the 2 cases the height
   fix alone didn't cover — the classic CSS Grid "item won't shrink below its
   own content" trap (a grid item's default `min-height:auto` lets its content
   win over its track size, growing the WHOLE grid row past budget): a stage's
   card in the `slide-stage-port-sample`/`-population` 2×2 grid (also given
   `white-space:nowrap` + ellipsis on its port-name column — a long name
   wrapping to 2 lines was the actual trigger), and `slide-closing`'s two
   grid columns.
3. **A missing flex wrapper** on `slide-s3-quality`'s Ledger variant
   (`.v2-lg-quality`) — it was a plain `height:100%` block box, which does NOT
   shrink block-flow children to fit; its mandatory caveat strip (a sibling
   after the 2-table split) rendered a few px past the wrapper's own bottom
   edge. Given `display:flex;flex-direction:column;min-height:0` + the split
   given `flex:1 1 auto;min-height:0` — the exact pattern `.v2-lg-workload`
   already used correctly.
4. **Raised the "narrow/mobile" reflow breakpoint from max-width:820px to
   max-width:900px** (both `deck/deckTheme.ts` and all 5 occurrences in
   `deck2/theme.ts`, kept in lockstep) after finding a second, narrower danger
   zone: even with fix #1, a 2-column layout (port tables, stage grids, etc.)
   forced into ~784px of actual width wrapped text badly enough to overflow
   dramatically (measured up to 778px of clipped content on one page) — the
   existing breakpoint's job is to abandon the fixed-card layout for natural,
   unclipped flow at exactly this point, but 820px cut it too close; 900px
   gives real margin, verified clean from 830px up through the full design
   width with no gap at the new boundary. **Discovered a second bug while
   fixing this**: the earlier height fix (part 1) had removed `aspect-ratio`
   from the unqualified height calculation, but the narrow-mode media query
   still only reset `aspect-ratio`/`min-height` (not `height`) — so narrow
   viewports were stuck at the new fixed 630px instead of getting genuine
   natural-flow sizing. Both media queries now explicitly reset `height:auto`
   too.

**Verified via a live worktree-scoped dev server** (`npm run dev`,
`deck-preview.html`, `variantPreview` mode) sweeping 830 / 850 / 870 / 900 /
901 / 950 / 1024 / 1440 px viewport widths, activating every one of the 21
slides' every variant panel exactly as `DECK_VARIANT_SCRIPT` does and measuring
real geometry — zero overflow at every tested width, in both the v1 (legacy)
and v2 decks (the fixed-height rule lives in the shared base file both share).

**File:** `src/data/reporting/executive/deck/deckTheme.ts` — `.slide`'s
`aspect-ratio` replaced with a fixed `height:630px`; its narrow-mode override
raised to `max-width:900px` and now also resets `height:auto`.

**File:** `src/data/reporting/executive/deck2/theme.ts` — all 5
`max-width:820px` media queries raised to `max-width:900px` (kept in lockstep
with the base file); `.slide.v2`'s narrow-mode override now also resets
`height:auto`; `.v2-stage-port-card` gains `min-height:0`/`overflow:hidden` +
port-name truncation; `.v2-closing-main`/`.v2-closing-side` gain
`min-height:0`.

**File:** `src/data/reporting/executive/deck2/section3/qualityImpact.ts` —
`.v2-lg-quality` changed from a plain `height:100%` block box to
`display:flex;flex-direction:column;min-height:0`, with its `.v2-lg-split`
child given `flex:1 1 auto;min-height:0` — the caveat strip sibling below it is
now actually accounted for in the wrapper's own layout.

**File:** `src/data/reporting/executive/deck/deck.test.ts` — "contains
landscape 16:9 print sizing" updated: the on-screen assertion no longer checks
for the now-removed `aspect-ratio:297/167` string; it checks for the new
`width:min(1120px,100%)` + `height:630px` pair instead (the print-mode
`size:297mm 167mm` assertion is untouched — print sizing was never affected).

**Lines:** 242569 → 242730 (net +161, incl. this edit-log entry itself) · 5
files, +177 / -16.

1295/1295 tests passing (156/156 files — the one stale byte-match test this
batch touched is fixed, not just now-passing-by-luck).
`typecheck`/`lint`/`check:hex-literals`/`check:bundle-size` all clean.

---

## v59.68 — 2026-07-28 — Fix (build): `npm run typecheck` was a silent no-op; fixed it and the 6 real type errors it had been hiding

User reported the admin design-customizer button as broken and asked for a final
check-up before merging. Investigation found no defect in the button's own wiring
(traced `handleOpenCustomizer` → `DeckDesignCustomizer` → the deck-wide segmented
control → `saveDeckStyleChoices`; `DeckDesignCustomizer.test.tsx` already exercises
this end-to-end with real `fireEvent.click` calls, not mocked handlers). What WAS
broken, and far more serious: `dist/index.html` — the single self-contained file
this app is distributed as — was last built 2026-07-23, five days and the entire
deck2 three-system fan-out effort before this session. Anyone opening that stale
file would see none of this week's work, including every fix already made to this
exact button.

Running `npm run build` to regenerate it surfaced 6 real TypeScript errors that
had never been caught by any of this multi-day effort's dozens of `npm run
typecheck` runs — because **`npm run typecheck` (`tsc --noEmit`) was a silent
no-op**. The repo's root `tsconfig.json` is a "solution" file (`"files": []`,
only `references` to `tsconfig.app.json`/`tsconfig.node.json`) meant to be built
with `tsc -b`, the mode `npm run build` already used (`"build": "tsc -b && vite
build"`). Invoking bare `tsc --noEmit` against that root config lists **zero
files** and exits 0 immediately — confirmed directly (`npx tsc --noEmit
--listFiles` → 0 lines; `npx tsc -b --listFiles` → 1029). CI's `typecheck` job
(`.github/workflows/ci.yml`) has been gating on this same no-op. This is why 6
real errors — 2 genuine unused-parameter violations repeated across 4 near-clone
Ledger table functions, 1 real `CellTone`-vs-`BriefingTone` type mismatch, and 1
typo'd test-fixture field — shipped across several already-reviewed commits
without a single `tsc` invocation ever actually inspecting the code.

**Fix:** `typecheck` script changed from `tsc --noEmit` to `tsc -b` (identical
mode `build` already uses) — verified it now checks all 1029 files and would have
caught every error below. The 6 errors themselves:
- `fanoutCoverSep.test.ts`: a fixture passed `selectedInSample: true` to `popRow()`,
  a field `PreparedPopulationRow` doesn't have (it's derived during
  `buildReportModel`, not settable on the raw input row) — removed; the fixture's
  `sample: null` input meant it was never doing anything.
- `ledgerQualityTable`/`ledgerAccuracyTable` (`slides.ts`), `ledgerLevelTable`
  (`section3/levelAccuracy.ts`), `ledgerAgreementTable` (`section3/portAgreement.ts`):
  all 4 took a `variant: "land"|"sea"` parameter, copied from their `portTableCard`-based
  slot-0 siblings, but build on `ledgerPortCard` — which has no icon/variant-driven
  behavior at all, confirmed by reading its signature. Genuinely dead in all 4;
  removed the parameter and updated both call sites per function.
- `qualityImpact.ts`'s Briefing rank-row builder passed `LEVEL_TONE[s.level]`
  (typed `CellTone`, which includes `"neutral"` — needed elsewhere in the same
  file for `qualCell`'s tinted cells) as a `BriefingRankItem.tone` (typed
  `BriefingTone`, no `"neutral"`). Verified `LEVEL_TONE`'s 3 concrete values
  (عالي→green, متوسط→gold, منخفض→coral) are always within `BriefingTone`'s range —
  a real subset relationship the type system just couldn't see from the wider
  declared type — so a scoped `as BriefingTone` cast at this one call site is
  correct, not a mask; documented why in a code comment.

Rebuilt `dist/index.html` (gitignored, not itself a tracked change) once the
build was clean.

**File:** `package.json` — `"typecheck": "tsc --noEmit"` → `"tsc -b"`.

**File:** `src/data/reporting/executive/deck2/fanoutCoverSep.test.ts` — removed
the invalid `selectedInSample` fixture field.

**File:** `src/data/reporting/executive/deck2/slides.ts` — removed the dead
`variant` parameter from `ledgerQualityTable`/`ledgerAccuracyTable` + both call
sites each.

**File:** `src/data/reporting/executive/deck2/section3/levelAccuracy.ts` —
removed the dead `variant` parameter from `ledgerLevelTable` + its call site.

**File:** `src/data/reporting/executive/deck2/section3/portAgreement.ts` —
removed the dead `variant` parameter from `ledgerAgreementTable` + its call site.

**File:** `src/data/reporting/executive/deck2/section3/qualityImpact.ts` —
scoped `as BriefingTone` cast (with comment) on `LEVEL_TONE[s.level]`; added the
`BriefingTone` type import.

**Lines:** 242340 → 242422 (net +82, incl. this edit-log entry itself + version
bump) · 8 files, +97 / -15.

`npm run build` now succeeds clean (typecheck via `tsc -b` + `vite build`, 1029
files checked). `npm run test:run`: 1295/1295 passing across 156/156 files.
`lint`/`check:hex-literals`/`check:complexity`/`check:vendor`/`check:release` all
clean at v59.68.0.

---

## v59.67 — 2026-07-28 — Add (executive-deck/deck2): fan-out slide-cover and slide-sep-1/2/3 to Ledger/Briefing/Grid (last 2 pages of the three-system fan-out)

Closes the deck2 three-system fan-out effort: the 17-page fan-out merged 2026-07-27
(`243c9f6a`) deliberately left `slide-cover` uncovered (an oversight in the original
plan, not a ruling) and `slide-sep-1/2/3` explicitly un-fanned-out (the plan's §4
argued a variant would be a pure shell reskin, forbidden by spec §5). The user's
follow-up instruction — "revise all pages and work even on separator pages and cover
etc" — asked for both to be done for real. A fresh Opus design-advisor dispatch
(`docs/superpowers/specs/2026-07-28-deck2-fanout-cover-and-separators-plan.md`)
corrected the prior separator ruling on its own terms: `.v2-sep` is the variant BODY
being re-composed on every other page in this deck, not the shell (eyebrow/headline/
subhead/rail/foot) spec §5 actually firewalls, and the true cost is one builder
(`sectionSeparatorSlide`) rendering all three separators, not "3 pages × 3 selectors ×
2 themes" as originally costed. What still stands, as a hard constraint: no
statistics return to the divider — the owner's earlier strip decision is not
reversed, and differentiation here is typographic/structural grammar only, over the
four fields the page already has (`sectionNo`/`title`/`blurb`/`tone`).

**`slide-cover`** (has full `ReportModel` access, unlike the separators — governing
rule: it may state SCOPE — population/sample/coverage, "what this document covers" —
never FINDINGS; nothing on a cover is honestly rankable, so no `briefingRankList`
anywhere on this page):
- **Ledger** — hero (title+lockup kept, kicker+badge dropped) beside one
  `ledgerTableCard` of 7 rows (فترة الدراسة/تاريخ الإصدار/الإدارة/القسم/مجتمع
  الصور/حجم العيّنة/تغطية العيّنة), each with `ledgerIdx`; no totals row (nothing to
  sum) — a classification footnote row instead.
- **Briefing** — title first (a cover's title cannot be demoted below a statistic),
  then `briefingLede` (population total as the SCOPE figure, no `arc` — coverage is a
  different unit than the lede's count), then `briefingSupport` (sample total /
  coverage / classification — the 3rd chip absorbs the dropped badge).
- **Grid** — new `gridFieldCells` primitive (label/value cells for pages with real
  fields but no comparable numeric domain — a cover is one entity with 4 strings + 3
  figures in 3 different units, so `metricMatrix` would be dishonest here; no
  `gridPanel` wrapper either, since its light-theme rule paints a white card, a hard
  break on the always-dark cover). 8 cells, 4×2, no magnitude tint (the 3 figures
  share no denominator — a tint would be invented magnitude).

**`slide-sep-1/2/3`** (input set closed — no `model`, no new `opts` field added):
- **Ledger** — deliberately NO table (no data exists; a one-row table would be
  theatre). A ruled document opener: hairline rule, `ledgerIdx(sectionNo-1)` +
  eyebrow, a smaller `<h2>` (2rem, vs slot 0's 2.6rem — a heading not a poster),
  another rule, a hanging-label definition line (`التعريف: {blurb}`), closing rule.
- **Briefing** — the cleanest reuse in the whole fan-out: one `briefingLede` call,
  nothing else (no support strip — nothing to put in it; no rank list). Figure =
  section number, `tone` mapped explicitly (`cyan → blue`) rather than passed through
  raw — `BriefingTone` has no `cyan` and there is no `.v2-bf-lede-figure.cyan` CSS
  rule, which the design plan flagged as a real bug this mapping avoids introducing.
  Page-local CSS bumps the figure to divider scale (6.4rem) and restyles
  `.v2-bf-lede-basis` from its default bordered-pill look (wrong for a ~30-word
  blurb) to a plain paragraph.
- **Grid** — one full-width `gridPanel` wrapping `gridFieldCells` (section number +
  the blurb as a wide cell) — the section number appearing in both the panel's `sub`
  line and a cell is this deck's own Grid idiom (every Grid page prints a column's
  domain in the header and its value in a cell), not a duplication.

`slide-month-numbers` stays explicitly skipped (dormant, `SHOW_MONTH_NUMBERS_SLIDE =
false` short-circuits the call site — slots 1-3 would never render in production or
preview either way).

**Highest-risk item, verified live**: the cover is dark in BOTH light and dark theme
by deliberate pre-existing design. Every new Ledger/Briefing/Grid rule for the cover
carries a matching `body.theme-light .slide.v2-cover …` re-override back to the dark
treatment, mirroring the pattern already used by `.v2-cover-meta-item`/
`.v2-cover-meta-value`. Verified in a live worktree-scoped dev server
(`deck-preview.html`, `variantPreview` mode, port 5188) by toggling
`document.body.classList.add("theme-light")` inside the deck iframe and reading
`getComputedStyle` directly: cover background stayed `rgb(6,40,70)` (navy) with
every new slot's text remaining white/slate (never flipping to dark-on-dark or a
stray white card) in both themes. Also verified live: no vertical overflow on the
cover's 7-row Ledger table or any separator's Briefing slot at the longest blurb
(section 3's), the cyan→blue tone-mapping fix (`slide-sep-2`'s Briefing lede
computes class `v2-bf-lede-figure blue`, never `cyan`), and that no slot-0 vocabulary
(`.v2-sep-watermark`/`.v2-sep-badge`) leaked into the 3 new separator variants.

**File:** `src/data/reporting/executive/deck2/slideKit.ts` — new `gridFieldCells`
primitive + its `GridFieldCell` type.

**File:** `src/data/reporting/executive/deck2/slides.ts` — `coverSlide` and
`sectionSeparatorSlide` gain real `ledgerBody`/`briefingBody`/`gridBody` variants.

**File:** `src/data/reporting/executive/deck2/theme.ts` — shared `.v2-gd-field*`
block (both themes) plus 6 page-local blocks (`.v2-lg-cover`/`.v2-bf-cover`/
`.v2-gd-cover`/`.v2-lg-sep`/`.v2-bf-sep`/`.v2-gd-sep`), each with its
`body.theme-light` form; the cover blocks additionally carry the
`body.theme-light .slide.v2-cover …` dark re-overrides described above.

**File:** `src/data/reporting/executive/deck2/fanoutCoverSep.test.ts` (new, 12
tests) — byte-identity slot-0 pins for both `coverSlide` and `sectionSeparatorSlide`
(neither existed before this batch), the cyan-never-passed-through tone-mapping
assertion, field-conservation checks (all 4 cover meta fields + classification
present in every slot), a no-rank-list check on cover Briefing, and a no-`<table>`
check on separator Ledger.

**File:** `docs/superpowers/specs/2026-07-28-deck2-fanout-cover-and-separators-plan.md`
(new) — the Opus design plan this batch implements.

**Lines:** 241323 → 241668 (net +345, incl. this edit-log entry itself + version
bump) · 8 files, +1021 / -6.

1282/1282 tests passing (155/156 files — the 1 failing file is the pre-existing,
worktree-only `Reports/index.test.tsx` Vite `fs.deny` font-asset environment quirk,
confirmed unrelated in every prior fan-out batch). `typecheck`/`lint`/
`check:hex-literals`/`check:bundle-size` all clean.

---
