# Edit Log — 2026-08-15

## v82.0 — 2026-08-15 — Fix (distribution): make the per-employee sample mirror a correct projection and refresh it after replacement

**Why:** An employee is meant to open their own work by reading one small per-employee file. That file existed but could not be trusted, for three separate reasons — so the expensive workspace-wide read stayed the primary path. Fixing the trust problem is the precondition for ever inverting that read.

1. **Bug F20 — the replacement flow never refreshed anything.** `executeReplacement` appended its events and returned. The other three write flows (`reopenAnswer:117`, `approveReferral:142`, `adhocImportAssignment:222`) all call `refreshDistributionCacheAfterWrite`; this one did not. So the flow that actually moves a row between employees left the derived cache **and every per-employee mirror** stale until the next 45 s tick.
2. **Bug F8 — a mirror could never recover.** `entriesByEmployee` was built only from `current.entries`, so an employee reassigned down to zero entries was never visited, their file never rewritten, and the `existingRevision >= sourceLogRevision` guard then blocked correction permanently. They kept seeing work they no longer owned.
3. **The mirror was not self-sufficient** — it carried no quota, so a reader still had to load the derived file anyway, which defeats the point.

**What changed:**

*F20.* `executeReplacement` now calls `refreshDistributionCacheAfterWrite` with `sampleResult.data.rows` — the sample master read back by `appendSampleRow`, so the freshest row set at that point, already including the appended replacement row.

*F8.* New `readExistingMirrors()` lists `2-employees/*.samples.json` and keys them by **file name**, not username — the monotonic guard is inherently per-file, and two usernames can sanitize to the same name. The write set is the union of on-disk mirrors and current assignees, so an employee who dropped to zero gets an explicit empty-entries file. The file is **emptied, never deleted**, keeping the mirror a stable read target. A directory-listing failure degrades to the old behaviour and reports via `logError` rather than failing the whole sync.

*Fan-out.* The unbounded `Promise.all` is replaced by the existing `mapWithConcurrency` (`src/data/storage/concurrency.ts`) at budget 8, for both the existing-mirror reads and the writes.

*Quota.* Optional `quota: { dailyQuota, daysRemainingAtAssignment, sampleCount }` copied from `current.quotas[username]`. The key is **omitted entirely** when there is no quota entry, so absent-`quota` mirrors keep working and readers fall back to the derived file — dual-read preserved.

*`main.samples.json` is no longer written.* Verified by grep across `src/` that nothing reads it: the only hits were the write site itself, its own revision guard, the golden test, a doc comment, and an unrelated `.samples.json` line. Nothing in `backup/`, `integrity/`, `powerbiExport/`, or any UI. The constant, the `MainSamplesFile` type and the now-unused `getSampleMainDir` import are gone, and `syncSampleMirrors` no longer touches `1-main/` at all (`saveDistributionCurrent` already creates that directory).

**Contract test, proven non-vacuous.** `sampleMirrorDropToZero.test.ts` drives all four write flows (distribute/assign, reopen, referral approval, replacement) and asserts an employee reassigned to zero ends with a zero-entry — not missing, not deleted — mirror. With the F8 union loop and the F20 refresh temporarily removed, **all four fail**; restored, all four pass. It is a real guard, not a restatement.

**Known limitation that matters for the next item.** Flows 2–4 refresh mirrors **asynchronously**: `refreshDistributionCacheAfterWrite` awaits `loadOrDeriveDistributionCurrent`, but the `saveDistributionCurrent` inside it is deliberately fire-and-forget (`void ….catch(logRejection)`) on both the checkpoint-resume and full-refold paths. Only distribute/assign awaits the mirror write. So the guarantee through reopen, approval and replacement is **eventual, not synchronous** — the contract test polls (bounded, 50×5 ms) rather than asserting once. Before the employee read path is inverted onto the mirror, this needs handling: a view refreshing immediately after one of those flows can still read the pre-refresh mirror for a tick.

**Golden-master diffs — every one deliberate, diffed case by case rather than re-recorded:**
- `pins the main.samples.json projection` → **no main file is written**; `1-main/` holds no files after a sync.
- `SURPRISE: main and employee monotonic guards use DIFFERENT comparisons` → the divergence is gone because one of the two guards was deleted; the surviving employee guard (`existing >= incoming` ⇒ skip) is unchanged and still pinned.
- `an employee who loses every entry keeps a STALE mirror` → **`FIXED (F8)`**: was `emp-a` stuck at `sourceLogRevision: 5` with `["img-1"]`; now `sourceLogRevision: 6`, `entries: []`, file present. This is the single largest intended behavioural change.
- New pair pinning the exact three-field quota snapshot (`username`/`assignedAt` deliberately **not** copied) and that an employee with no quota entry gets **no** `quota` key — the dual-read contract.
- Unchanged and re-verified: per-employee split content/order, missing-`logRevision` default of 0, file-name sanitization, the silent sanitize-collision surprise, and the verbatim row stub. `updatedAt` stays excluded.

**Migration/rollback:** On-disk shape changes in two backward-compatible ways. `{username}.samples.json` gains an optional `quota` — older readers ignore an unknown key, newer readers fall back when it is absent, so the file is readable both directions. `main.samples.json` stops being written; **existing copies are left in place** as dead files rather than deleted (nothing in this codebase deletes workspace data), so a rollback simply resumes rewriting them. No migration step is required in either direction.

**Docs now stale, outside the files touched here:** `CLAUDE.md:92`, `README.md:146,335`, `docs/architecture/data-system-report.md:137`, `docs/architecture/DATA_SYSTEM_FULL_MAP.md` and `AGENTS.md:37` all still list `main.samples.json` as written. `PERF_SYNC_ENHANCEMENT_2026-08-12.md:375`'s proposed "skip the `main.samples.json` write when nothing changed" optimization is now moot.

**File:** `src/data/distribution/replacement.ts` — F20 fix

**File:** `src/data/samples/sampleMirrorStorage.ts` — union write, bounded fan-out, quota, no main file

**File:** `src/data/samples/sampleMirrorStorage.golden.test.ts` — master updated, diffs above

**File:** `src/data/samples/sampleMirrorDropToZero.test.ts (new)` — four-flow contract test

**File:** `src/data/workspace/bootProgress.test.ts` — fixture label

**Before:**
```ts
// Only employees who still HAVE entries are visited, so a drop-to-zero is never written.
const entriesByEmployee = new Map<string, DistributionEntry[]>();
for (const entry of current.entries) { /* … */ }
await Promise.all([...entriesByEmployee.entries()].map(async ([username, entries]) => { /* … */ }));
```

**After:**
```ts
// Union of what is on disk with who currently holds work, so a drop-to-zero is emptied.
const existing = await readExistingMirrors(employeesDir);
const targets = unionOf(existing.keys(), entriesByEmployee.keys());
await mapWithConcurrency(targets, 8, async (username) => { /* writes [] when dropped */ });
```

**Verification:** verified in isolation from concurrent work by staging this change alone and stashing everything else. `npm run typecheck` clean; `npm run lint` clean; `npx vitest run src/data/samples src/data/distribution src/data/referral src/data/answers` — **25 files / 212 tests passed**. The full tier-3 sweep (build, bundle size, complexity, vendor, release) is recorded against v83.0, which was gated on the combined tree.

**Lines:** 5 files, +545 / -92

---

## v81.0 — 2026-08-15 — Refactor (workspace): cache resolved directory handles and key locks by full path

**Why:** Two independent costs in the same layer, both paid on every workspace read, and both invisible in a local test because they only hurt on a shared UNC/SMB volume where round trips dominate.

1. **Every read re-walked its directory chain.** Opening `2-samples/{month}/1-main/sample.master.json` re-resolved `2-samples`, then `{month}`, then `1-main` — 3–7 round trips per read, repeated for every sibling. The three sample sub-directories each re-walked `2-samples/{month}` independently.
2. **Every lock keyed on the leaf folder name.** `withResourceLock(\`${dir.name}/${fileName}\`)` produced the identical key `1-main/sample.master.json` for **every month**, so unrelated months serialized against one another for no reason.

**What changed:**

*Directory-handle cache.* A per-root memo of resolved handles keyed on logical path, with resolution wrapped in the existing `dedupeInFlight` from `src/data/storage/inFlightReads.ts`. `getRoot`'s numbered-vs-legacy probe is memoized separately, so a legacy workspace stops paying a failed `1-population` probe on every call — though `create: true` still deliberately bypasses a cached *legacy* handle, preserving "always create the numbered root". A new shared `getSampleSubDir` means sibling sub-directories cost one open instead of three walks.

*Lock-key registry.* `registerDirectoryPath`/`directoryPath`/`directoryResourceKey` live in `webLocks.ts` (the lock helper, not `safeWrite.ts`), backed by a `WeakMap`. Populated by `workspacePaths.ts` with the **logical** path — never the legacy folder name — and by `createMemoryDirectory` for test trees. An unregistered handle falls back to `dir.name`, i.e. exactly the old key. The registered path deliberately excludes any per-session workspace id: a too-coarse key only over-serializes, which is safe, whereas a key that differed between two references to the same folder would **under**-lock.

*Cache keyed on the root handle, not `workspaceScopeId(root)`.* `__clearInFlightForTests()` recycles scope-id strings, so a string key let one test's workspace answer for another's. A `WeakMap` plus a generation counter gives the un-iterable full purge.

**Invalidation** is the part that had to be right, since a stale handle reading a deleted-and-recreated directory is a correctness bug, not a slow path:
- `bumpWorkspaceEpoch` — month-scoped entries carry the epoch and are dropped lazily at lookup, so `inFlightReads.ts` needed no edit.
- Manual refresh — subscribes to `subscribeToDataRefresh` and purges on `"manual"` only (`window`-guarded, so worker/node contexts are unaffected).
- `NotFoundError` opening a child through a cached parent — the stale subtree is dropped and resolution retried once from a fresh chain.

**Known residual risk, deliberately not closed here.** The `NotFoundError` recovery is narrower than "any NotFound through a cached handle": it only fires where `workspacePaths` itself opens a child. A *file* read through a cached leaf directory whose folder was deleted externally still surfaces as "file missing" in `safeReadJson` and does **not** purge the cache. `invalidateWorkspaceDirCache(root)` is exported for whoever wires that in. Worth a follow-up before this is trusted against a workspace being reorganised underneath a running client.

**Also deliberately conservative:** epoch-based invalidation means every month *write* re-resolves that month's directories on the next read, even though directory topology cannot change because a file's contents did. Kept because correctness beats the remaining churn; a topology-aware epoch would remove it.

**Migration/rollback:** No data-format or on-disk change — this is resolution and locking only, both process-local. Rollback is a straight revert; nothing persists that a previous version could not read. Lock keys becoming *finer* cannot deadlock: the previous coarse key was a superset, so any pair that formerly serialized either still serializes or was independent all along.

**Budget tests updated, in the tightening direction.** Three exact-count assertions in `workspaceSync.test.tsx` moved because this change reduces what they measure: shared-parent opens **9 → 4**, the no-month cadence tick **2 ops → 1**, and `distinct.size` from `opens.length - 1` (one directory opened twice) to `opens.length` (none opened twice) — a stricter assertion than before. The marginal-cost test needed a second warm-up run: `runSync` bumps the workspace epoch at the end of a broadcasting run, invalidating month-scoped handles, so one warm-up left the cache unsettled and folded a re-resolution into the delta.

**Instrument correction:** the plan called for `getReadLog`, which only logs `getFile()` and therefore cannot see directory round trips at all. The assertions use `getOperationLog`/`trackOperations` from the same module, which logs `getDirectoryHandle`.

**File:** `src/data/workspace/workspacePaths.ts`

**File:** `src/data/workspace/workspacePaths.test.ts` — +7 tests

**File:** `src/data/storage/webLocks.test.ts` — +2 tests

**File:** `src/data/storage/memoryDirectory.ts` — import + one `registerDirectoryPath` call

**File:** `src/data/workspace/workspaceSync.test.tsx` — budget numbers tightened

**Before:**
```ts
// Re-walks 2-samples/{month} for every sibling, and every month shares one lock key.
const monthDir = await samplesRoot.getDirectoryHandle(monthFolderName, { create });
const mainDir = await monthDir.getDirectoryHandle("1-main", { create });
await withResourceLock(`${dir.name}/${fileName}`, fn);
```

**After:**
```ts
// Resolved once per root+path, deduped in flight; lock keys carry the full path.
const mainDir = await getSampleSubDir(root, monthFolderName, "1-main", create);
await withResourceLock(directoryResourceKey(dir, fileName), fn);
```

**Verification:** all tier-3 gates run and clean. `npm run lint` clean; `npm run typecheck` clean; `npm run test:run` — **249 files / 2224 tests passed**; `check:complexity` clean; `check:hex-literals` OK (no file over baseline); `check:release` consistent at v81.0.0; `check:vendor` SheetJS checksum verified; `npm run build` ✓ in 17.15s; `check:bundle-size` **3356.4 kB raw / 1120.6 kB gzip**, inside the 3.6 MB / 1.3 MB budget.

New coverage: a warm chain costs **0** directory round trips (3 cold); a sibling sub-dir costs 1; the legacy probe resolves once; an epoch bump invalidates only the bumped month; manual refresh purges while a periodic tick does not; NotFound through a cached parent self-heals and re-caches; a genuinely missing directory still throws; two months yield different lock keys and actually interleave under `withResourceLock`; an unregistered handle falls back to `dir.name`.

**Lines:** 5 files, +546 / -36 · whole repo now 274460 lines (excl. docs/edit logs/)

---

## v80.6 — 2026-08-15 — Add (tests): golden masters for the six deterministic-by-contract surfaces

**Why:** Sampling, distribution folding, and the report/export builders are deterministic **by contract**, and a large refactor is about to move underneath all of them. CLAUDE.md's rule is to snapshot before changing, never after. `distributionDerivation.ts` in particular had **zero** tests despite being the module that decides who is assigned what.

**What changed:** Five golden-master files, 79 tests, recording what the code *actually does today* — including behaviour that looks wrong. Nothing was fixed. Non-deterministic fields are excluded from each master (`drawnAt`, `exportedAt`, `updatedAt`); `runPowerBiExport` is pinned as exact CSV bytes via inline snapshot against a memory workspace.

**Suspected bugs pinned as observed behaviour.** These are recorded, not repaired — repairing one while snapshotting it would defeat the exercise. Highest-value first:

- **Sampling over-draws whenever a stage rule uses the `percentage` method.** `sampleAlgorithmInternals.ts:265/271` fills `configuredValues` from the raw `rule.value` regardless of `rule.method`, so `redistributeStageShortfall` (`:276-307`) compares a *percentage* against a *row count*: `value: 25` over 12 available rows reads as "13 rows short" and redistributes to compensate. In the fixture, stage 2 goes 3 → 12 and stage 3 goes 3 → 4 — both stages draw their entire population.
- **Phantom distribution events silently inflate employee quotas.** `distributionDerivation.ts:163`'s `if (!row) continue` absorbs an event whose `xrayImageId` is absent from the sample *before* `recordDroppedEvent`, so it lands in neither `droppedEventIds` nor `droppedImageIds`. Since `deriveEmployeeQuotasWithFacts` filters on `droppedEventIds`, those assignments still raise `sampleCount`/`dailyQuota`. (Related to, and worse than, the F15 entry already tracked.)
- **Power BI exports the wrong timestamp under `assignedAt`** — it is populated from `DistributionEntry.lastEventAt`, so a completed image exports its *completion* time in a column named `assignedAt`.
- **An employee who loses every entry keeps a stale mirror** — independent confirmation of the F8 defect (`entriesByEmployee` is built only from surviving entries, so a reassigned-away employee is never visited).
- Quota windows are set from **array order, not chronological order** (`firstAssignments[x] ??= event`): a later-timestamped assignment appearing first sets the window (28 days → 9 in the fixture). Assignment counting is also per *event*, not per image, so two `assigned` events for one image count twice.
- An unknown `eventType` is recorded as dropped **and still written**, advancing `lastEventAt`/`lastEventId` to an event the fold claims it discarded; `reopen-requested` on a completed entry likewise moves the timestamps while leaving the status `completed`.
- `isIllegalTerminalTransition` (`:52`) only blocks `eventType !== "replaced"` after a replaced entry, so a second `replaced` event re-applies and one with no `replacedById` erases the link back to null.
- `cleanConfirmationRate` mixes opposite verdicts: `originalClean = correctClean + excessSuspicious` (`executiveReportData.ts:238`) puts images the department called *suspicious* into the "originally clean" denominator.
- `lowQualityReasons` collects from both `منخفض` and `متوسط` but denominates on `lowQualityCount + mediumQualityCount`, so the percentages are shares of the quality-flagged population rather than of the reasons collected.
- Two usernames that sanitize to the same file name (`a/b` and `a\b` → `a_b.samples.json`) collide silently through the mirror fan-out; one overwrites the other.
- Spillover reconciles extra rows into `actual*` but not `allocatedQuota` (a port reports `actualTotalDrawn: 8` against `allocatedQuota: 7`), and can draw CertScan rows for a rule that explicitly set CertScan to 0%.
- The stage draw path returns `ok: true` with **zero rows** when all targets resolve to 0, where the legacy path rejects the equivalent — a caller checking only `ok` persists an empty `sample.master.json`.

**Determinism note:** `computeDaysRemainingForDeadline` builds its deadline with local-time `new Date(year, month, 0)`, so those assertions are UTC-implicit. This follows the existing precedent at `distributionLog.test.ts:375` rather than mutating `process.env.TZ`.

**File:** `src/data/distribution/distributionDerivation.golden.test.ts (new)` — 33 tests: `foldDistributionEvents`, `summarizeDistribution`, `findLateEvent`, `deriveEmployeeQuotas(WithFacts)`

**File:** `src/data/sampling/sampleAlgorithm.golden.test.ts (new)` — 19 tests: `drawSample` legacy + stage paths

**File:** `src/data/reporting/executiveKpis.golden.test.ts (new)` — 11 tests: `calculateExecutiveKPIs` incl. port/stage profiles

**File:** `src/data/powerbiExport/exportManager.golden.test.ts (new)` — 6 tests: `runPowerBiExport` exact CSV bytes

**File:** `src/data/samples/sampleMirrorStorage.golden.test.ts (new)` — 10 tests: `syncSampleMirrors` projection

**Verification:** `npx vitest run` on the five files — 5 files / 79 tests passed; `npm run typecheck` clean; `npx eslint` clean on all five.

**Lines:** 5 files, +2494 / -0

---

## v80.5 — 2026-08-15 — Change (performance): commit streamed writes from the verified staged copy instead of serializing the payload twice

**Why:** The streamed write path staged the payload to `.tmp`, verified it byte-exactly, and then **serialized the whole payload a second time** to write the live file. On the large files this path exists for, that second full serialization is pure duplicated CPU — the exact bytes had already been produced and proven correct one step earlier.

**What changed:** `verifyStreamedFile` was split into `readVerifiedStreamedFile`, which reads the staged file back, checks length **and** whole-file hash, and returns the verified text instead of discarding it. A new `writeVerifiedTextStreamed` commits that already-verified string through the same `streamToFile` machinery, emitting the same 64 KB chunks so the writable sees an identical chunk pattern and per-chunk hashing. `safeWriteJson`'s streamed commit now writes those verified bytes rather than calling `streamEnvelopeToFile` again.

**A second, undocumented instance.** `safeWriteJsonText`'s streamed restore path had the same double serialization (it re-serialized `parsed` via `streamValueToFile`). Both are fixed.

**Verification got stronger, not weaker.** The commit previously verified against the *second* serialization's own info — self-consistent but circular. It now requires `liveInfo.fileHash/fileLength === stagedInfo.*` **and** a byte-exact read-back against `stagedInfo`, which additionally proves the committed file equals the staged copy. The promotion path also drops one redundant full-file read by reusing `readVerifiedStreamedFile` instead of verifying and then re-reading.

The whole ladder is preserved: `.bak` snapshot first, stage → verify → commit → re-verify, `createWritable` still guarded inside `streamToFile`/`writeText`, `NotReadableError` retry and `retryMissing` semantics untouched. The small-file path is unchanged.

**On the size of the win:** the plan claimed this "halves the CPU of every large write." That is optimistic. The streamed path is only entered *after* `wrap()` has already run `hashJsonValue` — itself a full serialization pass — and after a failed `JSON.stringify`. So a real large write went roughly three full passes (wrap-hash, stage, commit) to two: about a third off, plus one fewer full-file read on promotion. Recorded here rather than repeating the plan's figure.

**Not done, and why:** a true zero-copy pipe is unavailable. `FileHandleLike`'s writable is typed `write: (data: string) => Promise<void>` and `memoryDirectory` concatenates strings, so there is no `Blob`/stream primitive to hand over without changing `fileSystemAccess.ts`. Copying over the verified JS string is also what keeps multi-byte UTF-8 intact — a byte-offset `Blob.slice` approach would corrupt Arabic text mid-character.

**Pre-existing, unchanged:** staging verification still materializes the whole file as one string via `file.text()`, so a payload beyond V8's max string length would fail there today regardless. This change neither adds nor moves that limit.

**File:** `src/data/storage/safeWrite.ts`

**File:** `src/data/storage/safeWrite.test.ts`

**File:** `src/data/storage/webLocks.ts` — adds `registerDirectoryPath`/`directoryPath`/`directoryResourceKey` (the item 1.11 lock-key helper), which `safeWrite.ts` consumes for its lock keys. Landed here because the two do not compile apart. Behaviour is unchanged until the path layer starts registering handles: an unregistered handle falls back to `dir.name`, exactly the previous key.

**Before:**
```ts
// Stage, verify... then serialize the ENTIRE payload again to commit it.
const stagedInfo = await streamEnvelopeToFile(dir, tmpName, envelope);
if (!(await verifyStreamedFile(dir, tmpName, stagedInfo))) { /* fail */ }
const liveInfo = await streamEnvelopeToFile(dir, fileName, envelope);
```

**After:**
```ts
// The verified staged text IS the bytes to commit — don't rebuild them.
const stagedText = await readVerifiedStreamedFile(dir, tmpName, stagedInfo);
if (stagedText === null) { /* fail */ }
const liveInfo = await writeVerifiedTextStreamed(dir, fileName, stagedText);
// Commit must equal what was staged, not merely be self-consistent.
if (liveInfo.fileHash !== stagedInfo.fileHash || liveInfo.fileLength !== stagedInfo.fileLength) { /* fail */ }
```

**Verification:** `npm run typecheck` clean; `npx eslint` clean on both files; `npx vitest run src/data/storage` — 18 files / 172 tests passed. Three tests added: a serialization counter asserting the commit phase consumes **0** passes (confirmed to fail with `expected 0, got 1` against the pre-change code, so it is a real guard); a byte-identity test asserting the live file's committed bytes equal the staged `.tmp` bytes, using Arabic text so a byte-offset copy would be caught; and a `safeWriteJsonText` streamed-restore round-trip.

**Lines:** 2 files, +199 / -29

---
