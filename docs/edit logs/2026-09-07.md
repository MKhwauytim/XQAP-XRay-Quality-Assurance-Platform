# Edit Log — 2026-09-07

## v132.3 — 2026-09-07 — Docs: add next-app comparison, enhanced plan and viability assessment

**What changed:** Added the three deliverables comparing XQAP against the proposed next-generation single-HTML architecture captured in v132.2: **A** the enhanced structure and plan (corrected shared-folder and source layouts, 24 ported practices, domain additions the study does not model, a migration/coexistence plan, an 11-phase programme with estimates, and a 20-row decision register), **B** the dimension-by-dimension comparison with a scorecard and cost analysis, and **C** the viability assessment (4/10 as a rewrite, ~7/10 as incremental adoption) with 13 evidenced mistakes fixed, what stays unfixed, 10 new risks the proposal introduces, and 13 kill criteria. Docs-only; no source, build, or test files touched.

**File:** `docs/next-app/A-ENHANCED-STRUCTURE-AND-PLAN.md (new)`

**File:** `docs/next-app/B-CURRENT-VS-PROPOSED-COMPARISON.md (new)`

**File:** `docs/next-app/C-VIABILITY-AND-MISTAKES-FIXED.md (new)`

**Verification:** `npm run lint` clean (exit 0); `npm run typecheck` clean (exit 0). No test file is affected — Markdown only. Every quantitative claim in the three documents was re-verified against this repo before commit; four were wrong in the drafting agents' output and were corrected: `DEFAULT_LABELS` holds **1,484** keys (not 1,366 — the object is flat, confirmed by walking it), label helpers are used at **307 `getLabels(` + 88 `useLabels(` call sites across 129 files** (not 109 call sites), and the edit logs carry **349** `Fix` entries (not 348). Claims that did verify exactly include 54 edit-log files / 961 entries, `backupStorage.ts` 2,042 lines, deck/deck2/deck3 at 1,361 / 14,140 / 2,103 non-test lines, `theme.ts` 2,289 lines, 260 `.tsx` files, the absence of `fast-check`, v61.0's 192,063 → 78 file ops, v87.0's 139.07 MB → 1.05 MB (132×), `bi.raw.json` at 573,236,797 bytes / 84.9% of V8's 536,870,888 code-unit ceiling, the 35.66× workspace inflation, and the ECharts **SKIP** verdict in `docs/research/VISUAL_LIBRARIES_2026-07-14.md`.

**Lines:** 3 files, +1606 / -0

---

## v132.2 — 2026-09-07 — Docs: capture proposed next-app single-HTML architecture study as comparison baseline

**What changed:** Added `docs/next-app/` and captured the owner's proposed next-generation single-HTML shared-folder architecture study verbatim as the fixed baseline that the forthcoming comparison, enhanced-plan, and viability documents are all written against. Docs-only; no source, build, or test files touched.

**File:** `docs/next-app/0-PROPOSED-STRUCTURE-SOURCE.md (new)`

**Verification:** `npm run lint` clean (exit 0); `npm run typecheck` clean (exit 0). No test file is affected — the change adds a Markdown document only. Run beyond the tier-1 requirement because repo CI is currently billing-blocked and could not validate anything: `npm run build` succeeded (`dist/index.html` 4,507.89 kB raw / 1,466.06 kB gzip, byte-identical to the committed artifact — the changelog generator excludes `Docs:` entries, so the bundle does not change); `npm run test:run` 4373 passed / 461 files.

**Lines:** 1 files, +1513 / -0

---
