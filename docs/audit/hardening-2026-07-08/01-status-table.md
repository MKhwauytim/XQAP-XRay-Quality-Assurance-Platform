# Milestone B4–E status check — 2026-07-08

Baseline for this check: `main` @ `5cf2ba89` (post referral-approval rework +
Tier-1 W1–W9 + hardening-pipeline docs). Audit source: `docs/audit/FULL_SYSTEM_AUDIT_2026-07-02.md`
§3 (Milestones B4–E). Cross-checked against `docs/audit/TIER1_SPEC_2026-07-05.md` and
`docs/audit/TIER3_SPECS_2026-07-07.md` — neither spec addresses these visual-polish /
completeness / hardening items directly (they cover a separate work track: month-lock,
audit trail, referral idempotency, reliability/AQL/gold-standard epics), so no items below
could be cross-verified against those two docs beyond confirming they're out of scope.

| Item | Status | Evidence | Notes |
|---|---|---|---|
| **B4** — Token sweep per CSS file (hex → `var(--…)`) | **Open** | `grep -oE '#[0-9a-fA-F]{3,8}'` counts: `EmployeeWorkspace.css` **310** hex literals (audit reported 287 — grew), `Reports.css` **197** (audit: 183 — grew), `DataTable.css` **133** (unchanged), `Population.css` **127** (unchanged). `src/index.css` has a token system (104 `--` custom properties, lines 38–87) but consumption in these files is unchanged. | Not started; the worst offenders got worse, not better, since the audit. |
| **B5** — Table polish (header truncation, number alignment, filter row) | **Partially fixed** | `DataTable.css`/`DataTable/index.tsx` already have `title` tooltips on truncated cells (`index.tsx:753,990`) and a `.dt-filter-date { min-width: 240px; }` rule — pre-existing mitigations noted in the audit itself ("partially exists"). Commit `0b6b18b5` (2026-07-05) "correct sticky-column offsets and default column order" landed post-audit. No explicit per-column default `min-width` tuning found (`grep minWidth` in `index.tsx` returns nothing) to specifically stop header truncation like "تار". | The sticky-offset/column-order fix is real progress but doesn't address the VIS-06 truncation case directly; no before/after screenshot evidence available in this pass. |
| **B6** — Spacing-rhythm pass (adopt spacing scale) | **Open** | `grep -n "^\s*--" src/index.css` → 104 custom properties, all color/brand tokens (`--brand-*`, `--c-*`); zero `--space-*`/`--spacing-*` tokens exist. `docs/UI_ENHANCEMENT_PLAN.md` Phase 1 (line 72–80) still describes this as future work, unmarked. | No spacing scale token layer exists anywhere in `src/index.css`. |
| **C1** — Seed demo data (realistic month in `demoWorkspace.ts`) | **Open** | `src/data/workspace/demoWorkspace.ts` (23 lines) is byte-for-byte the same shape flagged in the audit: `createWorkspaceStructure(handle, "viewer")` seeds only users; the docblock still reads "Richer seeded population/sample data can be layered on here later" (line 14). | No population/sample/answer seeding logic added. Demo mode still shows an empty product. |
| **C3** — First-run admin checklist | **Open** | `grep -ri "first-run\|firstRun\|onboarding\|checklist"` across `src/**/*.ts(x)` returns zero matches. `src/data/workspace/WorkspaceGate.tsx` (the first screen a new admin sees) still only shows a workspace-picker sentence and a "build structure" flow — no guided checklist UI. | Not started. |
| **C4** — Surface the error ring buffer (admin diagnostics) | **Fixed** | `src/components/Sidebar/Tabs/Settings/ErrorLogSection.tsx` (new component) calls `getRecentErrors()`/`clearErrors()` from `src/data/storage/errorLogger.ts`, gates on `role === "admin"` (line 16), renders a collapsible list with refresh/clear buttons, and is wired into the Settings tab (`Settings/index.tsx:36,378`). | Fully implemented and mounted; matches the C4 acceptance ("admin diagnostics panel: `getRecentErrors()` list + copy button" — clear/refresh present, no explicit "copy" button but functionally equivalent). |
| **C6** — Label coverage audit (hardcode → label keys in `App.tsx`/`WorkspaceGate`) | **Open** | `grep [أ-ي]` (Arabic-char literal detector) on `src/App.tsx` finds 10 hardcoded Arabic string literals still in place (e.g. line 164 `"وضع العرض التجريبي — للقراءة فقط…"`, line 227 `title="لا توجد تبويبات متاحة"`). `src/data/workspace/WorkspaceGate.tsx` has ~30 hardcoded Arabic strings across every screen state (`"متصفح غير مدعوم"`, `"اختر مساحة العمل"`, `"عنوان خاطئ"`, etc., lines 95–358) — none routed through `getLabels()`/`DEFAULT_LABELS`. | Zero progress since the audit; these are the exact two files the audit named. |
| **D1** — TEST-01 component/workflow tests | **Partially fixed** | `@testing-library/react`, `@testing-library/user-event`, `jsdom` are installed (`package.json`). Only 3 `.test.tsx` files exist app-wide: `src/auth/AuthGate.test.tsx` (real RTL render test of the login form, happy + failure path), `src/components/DataTable/stickyColumns.test.tsx` (logic-level, not a full render), `.../ReferralApproval/useApprovalData.test.tsx` (hook test). No tests for Population wizard, EmployeeWorkspace views (XrayReferrals, ReferralApproval UI), Reports, or TemplateBuilder. | Infrastructure is in place and one real workflow (auth) has coverage; the acceptance criterion ("1 happy + 1 failure path per workflow: import→process→sample→distribute→answer→report; characterization tests for DataTable & Population wizard") is far from met — 5 of 6 workflow stages and both named characterization targets (DataTable full render, Population wizard) are uncovered. |
| **D2** — XSS test on report builders | **Open** | `escapeHtml` exists in exactly one file, `src/components/Sidebar/Tabs/Population/reporting/reportHtmlBuilder.ts` (lines 8, 30, 35, 43, 52) — no test file exists for it (`find … -iname "*.test.ts*"` in that folder returns nothing). The larger, more complex executive-report pipeline (`src/data/reporting/executive/**`, used by the live Reports tab via `openExecutiveReport`) has **no** `escapeHtml`/`sanitize` function at all (`grep -rln "escapeHtml\|sanitiz" src/data/reporting` finds only the one Population file). No test anywhere injects `<script>`/HTML via port names or answers. | Both the "write a test" and (for the executive pipeline) the underlying escaping itself appear to be missing — higher-severity than the audit's framing suggests, since D2 assumed escaping existed and just needed a test. |
| **D3** — Import-mapping edge tests (extra/missing/renamed columns) | **Open** | No test file references `MappingSettingsModal` or column-mapping logic (`grep -l "MappingSettings\|columnMapping\|mapColumns"` across all `*.test.ts` → no matches). The one related test file, `src/components/Sidebar/Tabs/Population/processing/populationProcessor.test.ts`, covers column *preservation* and BI/risk-merge edge cases (6 tests, lines 109–253) but not the Excel-header mapping step (extra/missing/renamed source columns) that D3 specifically targets. | Adjacent coverage exists but not the mapping-layer edge cases named in the spec. |
| **D4** — OPS-01: CI (typecheck + lint + test + build) | **Open** | `.github/workflows/` contains exactly one workflow: `sbom.yml` (SBOM/CycloneDX generation only — explicitly scoped to dependency provenance, per its own header comment). No workflow runs `tsc -b`, `eslint`, `vitest run`, or `vite build` on push/PR. | Unchanged from the audit; only a security-adjacent SBOM workflow has been added since, which is a different concern (was not requested by D4). |
| **D5** — TEC-05: vendor `xlsx` tarball | **Open** | `package.json` still declares `"xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"`. No `vendor/` directory exists at the repo root (`ls vendor` → not found). | Unchanged; CDN dependency still live. (Per CLAUDE.md this is intentional/known, not accidental, but the audit's proposed mitigation — vendoring — has not been done.) |
| **D6** — SEC-01: written risk acceptance (`docs/SECURITY_MODEL.md`) | **Partially fixed** | No `docs/SECURITY_MODEL.md` file exists (`ls docs | grep -i security` → empty). However, `CLAUDE.md` now carries an explicit "Security model — advisory only" paragraph (added since the audit) stating the client-only trust boundary, that the bootstrap admin hash ships in the bundle, and that the app is not a defense against malicious insiders — functionally covering most of D6's content requirement. | The *content* of a risk acceptance now exists in CLAUDE.md, but not as the dedicated one-page doc the spec calls for, and it doesn't explicitly restate the TEC-06 viewer-passcode note or passcode policy in one place. |
| **D7** — Release process (version stamp in UI, CHANGELOG cut, build-size log, docs-sync checklist) | **Open** | `package.json` version is still the placeholder `"1.0.0"` (not tied to EDIT_LOG's v41.x/v42.x scheme). `grep -n "version" src/App.tsx` → no matches; no About/version display outside the ChangeLog tab's "latest version" stat (`ChangeLog/index.tsx:210`, which is a log reader, not a release-process artifact). No build-size logging step, no docs-sync checklist file, no CHANGELOG cut from EDIT_LOG majors. | None of the four sub-requirements are done. |
| **E1** — Accessibility pass (focus traps, keyboard walkthrough, contrast, `aria-*`) | **Partially fixed** | `role="dialog"` is present in 8 files (`AuthGate.tsx`, `ConfirmDialog.tsx`, `Archive/index.tsx`, `ReferralApproval/RequestList.tsx`, `ReviewModal.tsx`, `XrayReferrals.tsx`, `ReportDesigner/editor/FieldDropDialog.tsx`, `WorkspaceGate.tsx`) — reasonable baseline `aria-*` coverage, some likely pre-existing. No focus-trap utility exists anywhere (`grep -rln "focus-trap\|focusTrap\|trapFocus"` → zero matches), so keyboard users can tab out of open modals into the background page. No evidence of a contrast audit (no notes, no tooling config) or a documented keyboard-only walkthrough. | Baseline dialog semantics exist but the two concrete asks (focus traps, contrast check) are unaddressed; treat as started, not done. |
| **E2** — Reports/print pass (cover pages, print CSS, chart axis/legend) | **Partially fixed / mostly pre-existing** | Cover pages exist and are wired into the live path: `buildCover()` in `src/data/reporting/executive/document/frontMatter.ts:24` is called from `document/index.ts`'s `buildDocumentSlides`, which is invoked by `executive/index.ts`'s `buildExecutiveReport`/`openExecutiveReport` — the exact function the Reports tab imports (`Reports/index.tsx:14`). Print CSS exists in multiple report layers: `htmlReport.ts:40`, `sampleReport.ts:143`, `executive/theme.ts:594` (`@media print{`). However, `grep -n "legend\|axis\|Legend\|Axis" src/data/reporting/executive/ui/charts.ts` returns zero matches — the chart primitives (`rankedBar`, `gauge`, `donut`, `heatmap`) show no explicit axis-label or legend refinement work. | Cover/print-CSS portion looks substantially done (likely landed as part of the "dark-navy viewer" executive-report rework, not a dedicated E2 pass) but the chart axis/legend refinement named in the item is not evidenced. |

## Summary counts

- **Fixed:** 1 (C4)
- **Partially fixed:** 6 (B5, D1, D6, E1, E2, and D6 counted once — see note) — precisely: B5, D1, D6, E1, E2 = 5 partially fixed
- **Open:** 10 (B4, B6, C1, C3, C6, D2, D3, D4, D5, D7)

(1 fixed + 5 partial + 10 open = 16 total.)
